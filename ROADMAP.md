# Flight Sim — ROADMAP

Single-file Three.js flight sim shipping to https://sanjays2402.github.io/flight-sim/.

## Style rules (do not violate)
- Single `index.html` only. No build step, no bundler, no framework.
- Three.js via CDN import map already in the file.
- Keep everything self-contained. No new files except `assets/*` (sounds, textures) if absolutely needed.
- One feature per ship. Small, visible, playable improvement.
- Plain English commit messages: `feat(flight): <thing>`, `fix(flight): <thing>`, `polish(flight): <thing>`.
- No giant rewrites. Touch the minimum to land the feature.

## DONE
- [x] Engine prime hotkey (`Shift+I`) — cold-start now has a pre-prime ritual: hold Shift+I for 1.5 s to lock in a 30 s carb prime (green `✓ PRIMED <Ns>` pill on the WIND row that biases amber under 6 s left, audible carb-pump click). A fresh pre-prime lets a subsequent `I` tap skip the auto-prime cycle and catch in one clean spool. Force-starting (second `I` tap during the auto-prime) on a cold engine (CHT below `COLD_THRESHOLD_C` = 60 °C) now requires 2–3 crank cycles to light off, with a new `playCrankCough()` sputter SFX (low-passed noise burst + two descending sub-thumps) between attempts; warm engines + in-air deadstick restarts still light in one (no penalty). New state: `primeHoldT`, `primeExpiresAt`, `crankAttempt`, `crankAttemptsNeeded`, `crankCoughUntil`, all wiped on `armColdStart()` + `resetPlane()`. Cold-morning bias is latched at prime time, not crank time, so a pilot who primes at dawn keeps the chilly-spool penalty even if they crank a minute later. Defensive keyup + focus-blur abort so an accidental release or alt-tab doesn't auto-complete the prime. Status sub on cold-start updated from `press I to prime + ignite` to `Shift+I primes · I cranks`; help strip grew the new binding.
- [x] Mid-flight turbulence pockets — invisible 200–500 m bubbles drifting on the surface wind that inject body-frame pitch/roll/yaw jitter + a small vertical gust while the plane is inside one; smooth low (<500 m MSL) + high (>4500 m MSL), bumpy through the 800–2500 m mid-layer with feathered transition shoulders, severity scaled by per-pocket strength + steady-wind speed (calm day stays glassy, windy day bites). HUD pill (amber `〰 BUMPY` / red `🌪 SEVERE`) sits on the WIND row with a brief 0.9 s hold so a gap between adjacent bubbles doesn't blink it off; one-shot PILOT ATC `"Moderate chop reported, <FL>m."` (or `"Moderate-to-severe…"`) fires the first frame the plane crosses into moderate severity per altitude band and re-arms when leaving that band. Pool of 14 pockets respawns near the plane on a 35–110 s scheduler, hard-gated against crash/replay/photo/intro/pause/onGround (taxi stays steady), reset via `_chopResetLatches()` in `resetPlane()`. Pure additive — no new mesh, no new audio, no allocations per frame.
- [x] Basic takeoff + landing
- [x] Throttle-scaled engine sound
- [x] Bigger world: mountains, multiple airports, volumetric clouds
- [x] HUD redesign — proper attitude indicator with bank markers, compass strip, vertical-speed needle
- [x] Stall warning + buffet — visible warning, audio beep, slight screen shake near stall AoA.
- [x] Wingtip vortex trails when pulling Gs / near stall — short tapered ribbons.
- [x] Touch-and-go scoring — each successful landing gives points based on sink rate + centerline + speed at touchdown.
- [x] Free-flight mission picker — start menu with: Free Flight, Touch & Go Challenge, Airport Hop.
- [x] Airport hop mission — fly to the next airport in a sequence, finish timer.
- [x] Procedural cities near airports — boxy buildings for visual interest on approach.
- [x] Time of day cycle — sun moves, sky color, lights on runways at night.
- [x] Weather: wind + crosswind on approach — variable wind affects rudder.
- [x] Smoke / contrails behind plane at high alt or when damaged.
- [x] External chase cam + cockpit cam toggle (C key) — cycles chase/cockpit/flyby/orbit, HUD pill shows active mode, cockpit view hides own airframe.
- [x] Replay last 30s at the end of each landing — rolling 30s buffer of plane pose plays back after touchdown/crash, banner shows REPLAY, click or press R to skip.
- [x] Mini-map in HUD corner — top-down with airports, plane, heading line.
- [x] Damage model — over-G or hard-landing gives damage %, affects handling.
- [x] Engine-out glide — fuel burns with throttle, X toggles engine kill, prop windmills on deadstick approaches.
- [x] Refuel on ground at airport — taxi onto the apron, throttle to idle, fuel ticks back up (~30s for a full tank).
- [x] AI traffic — one other plane doing circuits at the home airport.
- [x] Mobile touch controls — virtual stick on left, throttle slider on right.
- [x] Settings panel — sensitivity, FOV, audio volume (persisted to localStorage, opens via ⚙ or `,`).
- [x] Liveries / paint picker — 5 color schemes (Classic, Navy, Forest, Sunset, Stealth) in settings panel, persisted to localStorage.
- [x] Ring-chase challenge mode — fly through a chain of 11 glowing rings against the clock, active ring pulses green, best time saved to localStorage.
- [x] Autopilot altitude + heading hold — press `H` to lock current altitude and heading (A is roll, so H = hold), HUD shows AP ON with locked alt/hdg, any stick input disengages, auto-off on ground or crash.
- [x] Persistent pilot stats screen — total flights, landings, crashes, best landing score, average score, longest flight, total air time. Opens from main menu via 📊 PILOT STATS button, stored in localStorage, reset with confirm.
- [x] Photo mode — press `P` to pause sim, free-orbit camera (drag + wheel), hide HUD, one-tap PNG download (`S` or SAVE button).
- [x] Gear and flaps sound effects + animated flaps — clunk on gear up/down, whoosh on flaps, flap surfaces visibly deflect on the wing.
- [x] Cockpit instrument panel — airspeed, attitude, and altimeter gauges drawn on small canvases at the bottom-center; only shown in cockpit cam (C key) so the cockpit view actually feels like sitting behind a panel.
- [x] Birds — small flocks of birds drift around low-altitude near airports, gentle flap animation, harmless but adds life to the scene.
- [x] Rain weather toggle — settings option for rain, adds tilted streaks across the screen, dims the sky toward overcast gray, persisted to localStorage. Hidden in photo mode so saved PNGs stay clean.
- [x] Landing gear stress indicator — HUD warning that flashes amber when sink rate is high enough to risk damage on touchdown, red when guaranteed damage, gives pilots a flare cue.
- [x] Achievement popups — quick toast in the corner when player hits milestones (first flight, first landing, 10 landings, perfect landing score 300+, ring chase under 60s, climbed above 1000m), stored in localStorage so each only fires once.
- [x] ATC chatter ticker — fake radio strip at top of HUD with rotating canned ATC lines tied to the current event ("Cleared to land 27", "Traffic, 2 o'clock", "Winds 240 at 12"). Pure flavor, big immersion bump for ~30 LOC.
- [x] Pause menu (Esc) — overlay with Resume / Restart Mission / Back to Menu, freezes sim + audio, also pauses the autopilot tick so nothing drifts. Currently there's no graceful way to bail mid-flight.
- [x] Runway taxi guidance arrows — when on the ground near an airport, draw a faint chevron line on the taxiway pointing to the active runway threshold so new players can find where to take off from.
- [x] Hard-deck altitude warning ("PULL UP") — when descending fast toward terrain below 150m AGL outside an airport zone, flash red "TERRAIN — PULL UP" with a sharp beep. Saves a lot of dumb crashes into mountains.
- [x] Spotter binoculars (B key) — hold B in external cam to zoom FOV way in with a subtle vignette, so you can actually look at distant airports, the AI plane, or rings before flying to them. Release to snap back.
- [x] Aerobatic ribbon trail (Y key) — toggleable colored smoke trail off the tail in the current livery accent color. Off by default, hidden in photo/replay/pause and on the ground. (Bound to Y instead of T because T already cycles time-of-day.)
- [x] Loop / roll detector — quietly track pitch and roll over time; when the plane completes a full 360° loop or aileron roll, fire an achievement-style toast ("LOOP!" / "AILERON ROLL!") and bump pilot stats. Tiny LOC, huge "wait, it noticed!" payoff.
- [x] Airshow smoke on/off button on the HUD — small `~` toggle (and tap target on mobile) that turns on a permanent colored smoke trail in the player's livery accent color. Pairs with the ribbon trail or stands alone.
- [x] Hot-air balloons drifting over the world — 4 lazy procedural balloons floating between airports, gentle bob, harmless. Pure scenery, makes the world feel less empty between airports.
- [x] Quick-restart hotkey (R outside replay) — when on the ground / crashed / paused, R restarts the current mission immediately (resetPlane + startMission, closes the pause overlay), and during a replay R also skips it and restarts. Mid-flight (healthy, in the air) keeps the legacy reset-to-runway so a stray R doesn't nuke an active sortie.
- [x] Wind sock at each airport — a small orange wind sock on a pole near the runway that tilts with the current wind direction and inflates with wind speed. Cheap to draw, makes pre-landing reads way easier than squinting at the HUD wind readout.
- [x] Crash report card — after a crash, a small panel pops up bottom-center with peak G, sink rate at impact, speed, AoA, the failure reason, and a one-line verdict ("STALLED ON FINAL", "GEAR-UP LANDING", "OVERSPEED IMPACT", etc.). Hidden in photo mode, click to dismiss, R restarts. Every wreck is now a lesson instead of just a replay.

- [x] Heading bug on the compass strip — press `[` and `]` to nudge a magenta tick around the compass; autopilot heading hold uses the bug instead of current heading. Plain key = 5° coarse, Shift = 1° fine. Off-edge chevron shows which way to turn when the bug is past the visible strip.
- [x] Engine RPM gauge in the cockpit panel — fourth small canvas next to airspeed/attitude/altimeter showing prop RPM scaled from throttle (drops with engine-out, climbs with throttle). Finishes the cockpit panel so it doesn't look lopsided.
- [x] Sunrise / sunset toast — when the time-of-day cycle crosses sunrise or sunset, fire a quick golden-tinted toast ("🌅 Sunrise" / "🌇 Sunset") so the player actually notices the world changing instead of missing it mid-turn.
- [x] Runway lights flare on touchdown — when the wheels actually kiss the runway, briefly pulse the runway edge/centerline lights brighter (yellow flash that decays over ~600ms). Tiny visual reward that makes every landing feel like a moment instead of just "score popup".
- [x] G-meter on HUD — live `G: 1.0` readout under SPD with a per-flight peak tick, turns amber past 2.5G and red past 4G. Players now see G-loads live instead of only learning from the crash card, so they can self-limit before snapping a wing.
- [x] Quick-jump airport selector (J key) — small overlay listing all airports with a one-key number; press the number to instantly teleport plane to that airport's runway threshold with engine running at 30% throttle. Free-flight only (disabled in scored missions, replay, photo). Massively shortcuts "I want to practice landings at the other field" without restarting the mission.
- [x] Wind direction arrow on the mini-map — small white arrow in the mini-map corner over a dim disc, shaft running FROM→TO, length scaled to wind speed. Players planning approaches from 5 km out can now read runway choice straight off the HUD without flying close enough to spot the airport wind sock.
- [x] Smoke trail color picker in settings — dropdown for ribbon/airshow smoke color (livery accent, white, red, blue, green, yellow, magenta), persisted to localStorage. Default LIVERY keeps the legacy behavior (color tracks the active livery accent) so no one's surprised; fixed picks let players brand their own air-show colors. Applies live to both the `Y` ribbon and the `~` airshow plume without rebuilding the scene.
- [x] Landing performance summary card — after every successful touchdown (not just scored missions), pop a compact panel with touchdown speed, sink rate, centerline offset, G at touchdown, rollout distance, and a one-line verdict ("GREASED IT", "FIRM", "PORPOISED", "OFF CENTERLINE"). Mirrors the crash report card so every landing is a learning moment, not just a score popup that vanishes.
- [x] Trim wheel (`;` nose down, `'` nose up) — adjustable pitch trim that biases elevator so you can fly hands-off in cruise. HUD shows a tiny `TRIM ▲▼` indicator with current setting. Pairs with autopilot (AP off, trim still works) and makes long cruises stop being a wrist workout.
- [x] Bird strike events — flying through a bird flock now triggers a wet thud, an ATC "Bird strike, bird strike" call, ~8% airframe damage, and a fading blood splatter on the windshield (cockpit cam only) over 4s. 1.5s cooldown per strike so a single fly-through doesn't multi-fire. Existing birds went from pure decoration to actual risk.
- [x] Approach glideslope indicator on the HUD — a vertical "meatball" / PAPI strip on the right side of the HUD that lights up red/white based on whether you're above, on, or below a 3° glideslope to the nearest runway threshold (only shown when within 5 km and roughly aligned with a runway heading). Reads like a real ILS without any of the radio nav complexity.
- [x] Flock formation flight — fly close + roughly parallel with the AI plane for 5 seconds to earn a "FORMATION!" toast + Wingman achievement; a thin green connecting line glows between the two planes while you're in the slot. Turned the existing AI traffic from background noise into something you actually interact with.
- [x] Repair on the apron — when refueling on the ground at an airport, also tick airframe damage back down toward 0 at roughly the same rate as fuel. Currently a hard landing scars the plane for the whole session even after a full pit stop; this turns the apron into an actual reset button and pairs naturally with refuel, no new UI needed beyond the existing fuel/damage HUD.
- [x] Sun glare lens flare — when the camera looks roughly toward the sun, draw a soft additive glare disc plus a faint streak across the frame, fading out as the angle widens. Hidden in photo mode so saved PNGs stay clean. Tiny shader-free overlay, makes sunrise/sunset flights feel cinematic instead of just "sky color changed".
- [x] Mini-map waypoint — click (or long-press on mobile) anywhere on the mini-map to drop a magenta waypoint pin; HUD shows bearing + distance to it, autopilot heading-hold can lock onto it with a new `M` shortcut (M instead of K because K is already pitch), second click clears it. Pin auto-clears within 300 m so it doesn't outlive arrival. Gives free-flight an actual goal without inventing a whole mission system.
- [x] Touchdown skidmarks — when the main wheels first contact the runway with any side slip or hard sink, paint two short dark streaks on the runway surface at the touchdown point that fade over ~30s. Greaser landings leave nothing; firm/crosswind landings leave visible evidence. Free visual feedback loop on every landing.
- [x] Cold start sequence — on mission start (and after engine-out restart), throttle is locked at 0 until the player taps `I` (or the mobile IGN button) to crank. Starter-motor SFX layered with a catch thump spools the prop for ~1.6s, then the engine fires and control returns. Settings toggle (default ON) for impatient pilots; X-restart on the ground re-arms the ritual so even emergency restarts feel like turning a key. Cockpit RPM gauge animates from 0 to ~300 RPM during cranking.
- [x] Mission completion fanfare — when an Airport Hop or Ring Chase mission completes, play a three-note ascending sine triad (C5-E5-G5 with a soft triangle harmonic) and flash a centered "MISSION COMPLETE" banner with the final time/score for ~3s. Banner pulses in with a pop animation, sits over the existing #status detail pill, and hides itself + the audio path in photo mode so saved PNGs stay clean. Reuses the stall-horn WebAudio context so no new hardware nodes are allocated.

- [x] Fuel gauge warning at 20% — fuel HUD now flashes amber below 20% with a one-shot ATC "Fuel state low, recommend divert" call, flashes red below 10% with a second "Fuel critical, land at nearest field" call, and the latch clears with a small hysteresis once you refuel above each threshold so the next sortie re-arms.
- [x] Cockpit head-bob — in cockpit cam only, the camera now picks up a subtle low-frequency wobble: tiny engine-vibration shimmy that scales with throttle in the air, much rougher bump scaled by ground speed during taxi, and a sharp decaying jolt on every wheels-down (scaled by sink rate, so greasers feel like a kiss and slammers thump the chassis). Stall buffet damps the bob so the two don't fight each other. Hidden in photo/pause/replay/intro so saved PNGs and frozen frames stay perfectly still.
- [x] Wind sock at airports — NEXT entry was a duplicate of the already-shipped "Wind sock at each airport" item above; closed out without re-implementation so the same feature doesn't ship twice.
- [x] Fuel-flow gauge in the cockpit panel — NEXT entry was a duplicate; the `cpFf` canvas + `drawCockpitFuelFlow()` gauge already ship in the panel (idle ~2 GPH / cruise ~9 GPH / full ~16 GPH, mixture-aware so the needle drops on a leaned high-altitude cruise, ramp during cold-start + air-restart spool, engine-out reads 0). The matching fuel-range ring (DONE) and mixture-lever (DONE) entries both reference it as already shipped, so closed out without re-implementation to avoid double-ship.
- [x] Reverse thrust on landing — hold `Z` on the runway after touchdown to engage reverse thrust: prop disc visibly tilts ~0.35 rad to read as inverted pitch, a one-shot whoosh + low rumble plays on engagement, friction jumps hard (stacks with `B` brakes) so rollouts shorten dramatically, and tan dust puffs spawn at the main wheels and fade over ~0.9s. Disengages below ~30 km/h ground speed and outside the runway so the reverser can't shove the plane backwards or fire in a field. HUD shows a ◀ REV pill that eases in/out with the visual lerp.
- [x] Smoke trail for the AI plane — thin white plume off the AI circuit plane (reuses the airshow sprite pool pattern with smaller pool, lighter emit rate, lower peak opacity). Always white so it reads as "not you" against the sky, and gated by aiPlane.visible so it dies out cleanly when the AI plane culls at 12 km. Spots the other plane from 3+ km out at a glance and makes formation work way easier to set up.
- [x] In-air ghost replay for Ring Chase — every time you set a new best time on Ring Chase, sample plane pose every ~150ms into localStorage. On the next attempt, render a translucent ghost plane following that exact path. Turns the stopwatch into a race against yourself; reuses replay-buffer plumbing.

- [x] Opposite-end runway in quick-jump (Shift+digit) — the `J` overlay teleports you to one canonical threshold per airport. Hold Shift with the digit (or Shift+click the row) to drop in at the *other* end of the same runway with the plane already rotated 180°, so wind-favored takeoff is one keystroke instead of a long backtaxi. HUD pill reads `JUMPED TO <id> (REV)` and the ATC line tacks on "opposite end" so it's obvious which way you're pointed.

- [x] Carrier deck mini-mission — anchored CV-01 a few km offshore (240×38 m deck at 14 m elev) with hull, markings, four arrestor wires, a touchdown box, and an island superstructure. New CARRIER mode on the intro screen sets a trap inside the ±40 m midship box as the win condition; landing anywhere on the deck adds an arrestor-strength friction multiplier (`+7.5`) so the rollout is brutally short, hook or no hook. Mini-map gets a `CV` symbol; glideslope tape still works thanks to the deck registering as a synthetic runway.

- [x] Weather radar mini-panel — small dark canvas next to the mini-map showing top-down fuzzy blobs for current cloud-layer positions plus a soft green sweep wedge. Doesn't need to be physically accurate — it just has to read as "weather radar" so the player can see which way weather is drifting in the wind. Pairs naturally with the existing rain toggle.

- [x] Engine fire emergency — rare random event (or triggered by a bird strike at high throttle) sets the engine on fire: orange flickering sprite at the cowl, black smoke pouring off into a long plume, fast-rising airframe damage at ~6%/sec, ATC "Mayday, engine fire, declaring emergency," and a flashing red 🔥 ENG FIRE banner with a repeating two-pulse chirp. To put it out: cut the engine with `X` and drop throttle to idle — the fire starves over ~10s. Ignore it and the airframe melts through 100% into the existing airframe-failure crash branch. Turns a quiet sortie into a real deadstick scenario you have to handle, and gives the bird-strike system actual teeth.

- [x] Runway condition overlay — when rain is on, every runway, apron, and the carrier deck re-tint toward a darker, slightly mirrored shade (live, no scene rebuild) and braking friction on the runway drops noticeably (brakes ~45% weaker, free-rolling friction ~30% weaker) so rollouts are longer and skids are easier. HUD now shows a blue `RWY WET` pill next to the WIND row so pilots can read the slick before they're on the strip. Carrier deck is exempt from the friction drop (the arrestor wires don't care about a shower) but still tints, so visual + gameplay stay consistent.

- [x] Quick-look check (`V` key) — in cockpit cam, hold V to swing the head ~90° left and release to ease back; tap-tap V toggles a persistent 90° right look-out so you can hold the wing view during a long crosswind base. Eased yaw rotates the look-at around the plane's up axis so the horizon stays level. Gated to cockpit only and disabled in intro / photo / replay / pause / crash so it can't fight other modes. Added to the help strip and the keydown preventDefault list.

- [x] Airport beacon lights — each airport now has a slim 8m tower opposite the windsock with a slow-spinning green/white head pair. Heads emit base glow gated by the same dusk window as runway edge lights (off at noon, on at sunset through dawn) and carry additive glare sprites that pulse as each head sweeps toward the camera, so the airport reads from 8+ km out at night without spamming daylight skies. ~4.5s rotation with a per-airport phase offset so the three beacons don't blink in lockstep.

- [x] Departure / arrival callouts — `onTakeoff()` now looks up the departure runway via `landingProj` at threshold cross and fires `"<id>, airborne runway <DD>, contact departure."` (TWR), with a heading-aware `runwayDesignator()` helper so an east-rolling departure off RWY 09 reads "09" and a west rollout off the same strip reads "27". Off-field departures (carrier, grass, taxiway) fall back to the existing generic line so the call always fires. The rollout-complete latch in `updateLandCardRollout()` now fires a matching arrival call (`"<id>, cleared to taxi, welcome in."`) the first frame ground speed drops below 0.8 m/s, gated to crashes-off + onRunway so wreckage and off-strip stops stay quiet.

- [x] Approach speed callouts — `RA` ATC strip and a soft sine chime fire each time AGL crosses 150m ("500"), 90m ("300"), 60m ("Minimums"), 30m ("100"), 15m ("50"), 9m ("30"), 6m ("20"), 3m ("10") on the way down to the nearest runway. Gated to gear-down, descending, within 2 km of the threshold, aligned within ±250 m of centerline, and not crashed/replay/photo. Re-arms above 200m AGL so every touch-and-go pattern gets a fresh sequence. Reuses the existing `pushATC` ticker and `sfxReady()` WebAudio path — no new assets, ~50 LOC.

- [x] Flight log book — every completed flight (takeoff to full-stop or crash) writes a row to localStorage with timestamp, mission type, route (origin → destination), duration, max altitude, landing score, and verdict. New 📖 LOGBOOK button on the main menu opens a scrollable panel with the last 50 flights, oldest scrolled off. Pairs with the existing pilot stats screen so players can actually see their journey instead of just aggregate numbers.

- [x] Wake turbulence behind the AI plane — when you fly within ~80 m behind and ~30 m below the AI circuit plane, the player gets a roll + yaw kick scaled by proximity plus a one-shot low rumble SFX (with a 1.6s cooldown so re-entries don't fire-spam). Faint white vortex ribbons trail off the AI plane's wingtips (same shape as the player vortex, lighter alpha) and pulse brighter while you're actually in the wake. All of it culls cleanly with the existing 12 km AI draw distance and hides in pause/replay/photo/intro/crash. Turns AI traffic from background scenery + formation target into a genuine hazard pilots learn to respect.

- [x] Crosswind landing crab indicator — on final within 3 km of a runway and below 200m AGL, HUD shows a small horizontal bar below the ATC strip with a fat green tick marking the runway centerline heading (active runway designator labeled above it) and a magenta arrow showing the plane's current ground track. Visual gap between them IS the crab angle, and a `7° L CRAB` readout under the bar color-codes from green (aligned) through amber to magenta as the offset grows so pilots can kick out rudder at flare instead of guessing from the wind readout. Tries both runway ends so opposite-end landings (Shift+J jumps) also light it up. Off-scale chevrons mirror the heading-bug pattern, falls back to a `— CRAB —` placeholder under ~5 m/s ground speed so the arrow doesn't jitter on taxi, and the whole bar hides outside approach + in photo mode.

- [x] Engine temperature gauge — fifth small canvas in the cockpit panel showing CHT (cylinder head temp) starts cold at 40°C and lerps toward a throttle/climb/altitude-driven target each frame (idle ~95°C, cruise ~180°C, full power + sustained climb pushes into the 230°C+ redline). Gauge mirrors the RPM gauge style with green cruise arc (95-180°C) and red redline arc (230-260°C). HUD shows amber `⚠ ENG TEMP` while CHT is in the redline band, and upgrades to red flashing `⚠ ENG TEMP — REDUCE POWER` once sustained overheat (>15s) latches; that latch also fires a one-shot ATC "Engine overheat, reduce power" call and starts ticking 0.3%/sec airframe damage. Latch clears with hysteresis once temp drops back under redline-10°C so brief excursions don't punish. Engine off / cold-start / fuel-out cools toward ambient, so the gauge has real personality across a flight.

- [x] Oil pressure gauge tied to engine fire / overheat — sixth small canvas in the cockpit panel showing oil pressure (PSI) that sits steady ~65 PSI at cruise, drops fast when the engine is on fire or overheated past the redline latch, and bottoms out to 0 on engine kill. HUD adds a one-shot ATC "Oil pressure dropping" call the first frame it crosses below 25 PSI, and pressure recovers as the engine cools / restarts. Tiny new visual, but it finally explains *why* a fire is bad before the airframe damage spikes — pilots get a leading indicator instead of a lagging one.

- [x] Pattern entry guide — when within 5 km of an airport and below 1500m AGL in free flight or touch-and-go, the mini-map now draws a faint dashed magenta box tracing the standard left-hand traffic pattern (downwind / base / final / crosswind) on the left side of the active runway, ~1.5 km out and ~2 km past either threshold. Fades in past 800 m and out beyond 4 km so it never fights the runway line on short final. First approach to each airport per session fires a one-shot 🛩️ "<id> PATTERN — Enter on 45° to downwind (left-hand traffic)" toast. Teaches real pattern flying without any nav-radio complexity, pairs naturally with the existing ATC ticker + crab indicator.

- [x] Multi-engine prop count toggle in settings — new ENGINES row in the settings panel flips between SINGLE (nose prop) and TWIN (two smaller wing-mounted props with short bullet nacelles at ±2.2 m, just forward of the wing leading edge). Visual swap only — same flight model, same total thrust, same engine-fire / overheat path. The starter-motor SFX in `playIgnitionCrank()` now layers a second whir + catch pop at +7% detune when TWIN is on so cranking reads as two cylinders spinning up together. Cockpit view keeps both layouts owned by `applyEngineConfig()` so toggling between chase/cockpit doesn't fight the hide loop. Persisted to localStorage, default OFF so existing flights look identical until the player opts in.

- [x] Wind shear on short final — randomly (15% chance per approach) below 60m AGL within 1 km of a runway threshold, inject a brief 4-6 second wind-direction flip + speed surge (one of two patterns: a ~180° flip with a +4-8 m/s bump for a sudden headwind drop / tailwind kick, or a 60° crosswind swing with a +6 m/s surge). HUD flashes a yellow `⚠ WIND SHEAR` pill in the WIND row and ATC fires a one-shot "Wind shear on final, RWY <DD>" call using the existing `runwayDesignator()` helper so the named runway matches the threshold the plane is actually shooting for. Triggered latches per approach and re-arms once AGL climbs back above 200m (mirrors the radalt callout pattern). Pairs naturally with the existing crosswind crab indicator so the magenta arrow goes wild and the pilot has to react.

- [x] Airport restaurant / "100-dollar hamburger" mini-objective — one of the three ground airports is designated OPEN FOR LUNCH each session with a pulsing 🍔 next to its mini-map label (and a tiny 🍔 glyph next to its rim tick when off-map). Full-stop a landing there (any mode, any runway) to score a one-shot "🍔 100-DOLLAR HAMBURGER!" toast + bump a permanent `BURGERS EATEN` counter in pilot stats. Designation rotates to a different field after each eat so there's always a fresh destination. Piggybacks the existing rollout-stop latch so no new physics, no new UI plumbing — just a soft goal hook for free-flight pilots and the joke name every real pilot will instantly recognize.

- [x] Magnetic compass card in the cockpit panel — seventh small canvas next to the existing cockpit gauges showing a rotating compass card (the classic whiskey-compass look: floating drum with N/E/S/W cardinals and 30°/60° tick labels visible in a window). Card spins to current magnetic heading and includes the realistic ANDS lag (Accelerate North, Decelerate South — slight overshoot/undershoot on east/west turns when accelerating) for ~15 LOC of charm. Pairs with the existing compass strip up top so VFR pilots have a real backup if the HUD ever gets occluded.

- [x] ILS localizer + glideslope needles when within 8 km of a runway — square cross-pointer pinned under the meatball strip on the right edge: magenta vertical needle for localizer (left/right of the extended centerline, swings the way you need to fly to recapture course) and green horizontal needle for glideslope (sits high when you're above the 3° slope, low when you're below). Reuses the existing glideslope/centerline math from the meatball + crab indicator at a wider 8 km range and slightly looser alignment cone. Off-scale chevrons echo the meatball pattern, runway id labeled in the corner. Hidden in photo mode.

- [x] Wake turbulence behind the AI plane (re-entry) — NEXT entry was a duplicate of the already-shipped "Wake turbulence behind the AI plane" item above; the `updateAIWake()` path (WAKE_BEHIND_M = 80 m, WAKE_BELOW_M = 30 m, asymmetric roll/yaw kick scaled by proximity, one-shot low rumble with 1.6 s cooldown, faint vortex ribbons off the AI wingtips, gated by AI plane visibility + pause/replay/photo/intro/crash) already ships. Closed out without re-implementation so the same feature doesn't ship twice.

- [x] Carrier landing arresting wire physics — NEXT entry was a duplicate of the already-shipped trap logic; `state.arrestor` (init around `state.arrestor = { active:false, wireId:0, t:0, dur:0.5, decel:0 }`), wire-catch detection in the touchdown branch (`ARRESTOR_CATCH_M = 6.0` around the canonical four-wire grid at deck-local lz `-12, 0, +12, +24`, tailhook + gear-down + sink ≤ 6 m/s gating, direction-aware ordering so wire #1 is always the aft-most crossed), the 6 g / 0.5 s horizontal snap (`decel = max(58.86, groundSp / 0.5)` applied in the post-touchdown ground-physics block), the one-shot `playArrestorClang()` metal-on-wire ring (filtered noise + sub thump), per-wire pilot ATC + status pill (`"Three wire — perfect trap!"`, `TRAP · WIRE #N · OK!`), the carrier-mission win latch when the catch lands inside `±boxHalfLen` of midship, and the lifetime `pilotStats.carrierTraps` + `pilotStats.bestWire` bank (with `stTraps` / `stBestWire` rows already wired in the PILOT STATS panel as `⚓ CARRIER TRAPS` and `⚓ BEST WIRE`) all already ship. Closed out without re-implementation so the same feature doesn't ship twice.

- [x] Rudder trim (`Alt+;` left, `Alt+'` right) — a second axis on the existing pitch-trim system. New `state.rudderTrim` (-1..+1, +1 = full right-rudder bias) is added to `yIn` in the same flight-physics gate as pitch trim (skipped while AP heading-hold is on, on the ground, or after a crash) so a torque-rolling or wing-heavy crosswind cruise stops being a constant boot-on-pedal workout. Plain Alt = 5% coarse, Alt+Shift = 1% fine. Resets to 0 in `resetPlane()` alongside pitch trim. HUD's TRIM row now carries a second segment `RUD ◀▶ ±NN%` that lights up independently — pitch and rudder each hide when within the neutral dead-band, so a pilot only trimming one axis doesn't see a stale `0%` on the other. Spec asked for `Shift+;` / `Shift+'`, but both are already shipped (`Shift+;` = cockpit night mode, `Shift+'` = fine pitch nose-up trim); per the "never delete existing features" rule, swapped to Alt — only unused modifier on the `;`/`'` keys — and updated the help strip with `Alt+;/' rudder trim`.

- [x] Crosswind takeoff aileron-into-the-wind coaching — during the takeoff roll on a runway with crosswind component >4 m/s, a magenta arrow appears in the lower-center of the screen pointing the way the pilot should hold aileron (into the wind, so the upwind wing doesn't lift) with an `AILERON LEFT/RIGHT` + `CROSSWIND` label. Direction is computed from the plane's actual nose vector dotted against the wind so the cue stays correct on either runway end. ATC fires a one-shot `"Aileron into the wind, RWY <DD>"` (TWR) line via the existing `runwayDesignator()` helper so it names the threshold actually being rolled, latched per takeoff so a long sit on the numbers doesn't spam the strip. Opacity fades smoothly to zero as ground speed crosses 60 km/h (with a 15 km/h ease band) so the arrow doesn't nag through rotation, then re-arms once airborne for a fresh ground roll on the next sortie. Hidden in photo / pause / replay / intro / crash via the existing photo-hide list. Pairs with the wind sock + crab indicator to teach the single most-missed crosswind technique.

- [x] Engine restart sequence after deadstick — X-toggle engine kill is unchanged, but restarting from `engineOut` while airborne now requires throttle <30% + airspeed >80 km/h (windmilling prop) and pressing `I` to crank, mirroring the cold-start ritual: 1.6s starter motor SFX, catch thump, engine fires. New `state.airRestart` latch arms on a successful X-press in flight (HUD shows `IGNITION ARMED — press I to crank`, ATC "Restart attempt, cranking on your call."); if throttle/airspeed conditions aren't met, ATC fires "Restart failed, check throttle and airspeed." instead and nothing arms. Shared `beginIgnition()` / `finishIgnition()` / cockpit RPM spool with cold-start so the ritual feels consistent in the air and on the ground; ground-with-coldStart-OFF still hot-starts instantly. Mobile IGN button + help row updated.

- [x] Hangar / parking spot at each airport — every airport now has an open-front hangar building (~12m wide × 8m deep × 6m tall with a curved half-cylinder roof and a dark dragway-style front frame) sitting ~24 m off the runway edge on the opposite side from the taxi-chevron apron, well clear of the windsock and beacon. One static AI plane is parked inside in a livery deterministically picked to differ from the player's current paint, with a frozen prop and the same silhouette family as the AI circuit traffic. Pure scenery — no collision, no interaction — and a small per-airport z jitter so the three hangars don't look like an assembly line if you fly between them.

- [x] Wing flex on positive Gs — wingtip blocks are now mounted on inboard pivot groups at ±3.8 m so the rendered tip face stays at ±4.3 m when relaxed; per-frame the pivots lerp toward a target of ~1°/G above 1G (clamped at ~4° / 0.07 rad) so hard pulls and pullups visibly bend the tips upward. Hidden in cockpit cam via the existing airframe-hide loop, kept in photo mode so action shots get the bent wing, snaps flat in resetPlane(), and target collapses to zero on engine-out / cold-start / fuel-out so quiet glides stay calm.

- [x] Bird strike windshield crack (cockpit-cam) — a bird strike at >180 km/h now layers a thin two-pass jagged crack on the windshield overlay (dark core + thin white highlight + small impact chip) with 2-3 branching forks off the impact point. Cracks persist for the rest of the sortie (cleared in `resetPlane()` alongside any leftover blood splatter), draw underneath the existing red splatter so the wet smear reads as sitting on cracked glass, and pick up subtle parallax off the cockpit head-bob clock so the damage feels attached to the windshield instead of painted onto the camera lens. Cockpit-cam only, hidden in photo mode, splatter fade timer is now decoupled from the canvas-show check so a faded splatter doesn't drop the canvas while cracks remain.

- [x] Wing-wave greeting with the AI plane — fly within 250 m of the AI circuit plane (any mode) and bank past ±25° three taps in a row alternating sides (e.g. R→L→R) inside a 3-second rolling window to fire a one-shot 'WAVE' toast, ATC echo ('Good wave, see you out there.'), and a 1.5 s eased two-cycle wag-back on the AI plane (±20° peak, faded in/out via an envelope so it reads as a deliberate greeting and not a wing-rock). Achievement-style toast fires once per session; new permanent 'WINGS WAGGED' counter in the pilot stats panel ticks every greeting. Detection re-arms whenever the player levels back inside ±8° so a long held bank only counts as one tap, and a 12 s cooldown after each fire keeps social toasts from spamming. Gated to airborne + non-paused/photo/replay/intro/crash and culls cleanly with the existing 12 km AI draw distance. Turned the AI traffic from a formation target into a real social moment that every GA pilot recognizes.

- [x] Tower light-gun signals (no-radio comms) — when stationary on a runway with the engine running for 6+ seconds ("holding short with a dead radio"), the nearest airport beacon fires a one-shot 2-second directed colored flash at the player: solid green (~70%, cleared for takeoff), flashing red (~20%, hold position / traffic), or white (~10%, return to start / taxi back). ATC ticker echoes the matching real-world phraseology. Re-arms once the wheels are rolling again (gs > 1.5 m/s) so the next holding-short cycle gets a fresh signal. Gated to engine-live + on a runway threshold (not taxiway/apron/grass) + not in pause/photo/replay/intro/crash. Reuses the existing beacon meshes — both heads override to the chosen color for the flash window with a red flicker, then snap back to the standard green/white sweep — so no new geometry, no new tower, just the rotating beacon finally doing what its real-world counterpart does.

- [x] Wind direction labels on the windsock pole — a tiny world-aligned compass-rose disc (~1.2 m / N•E•S•W cardinals + 30°/60° labels + 10° tick marks + bolder 30° ticks) is now painted on the ground at the base of every airport windsock pole. Cancels the runway-local yaw so N is geographic north no matter how the strip is oriented, sits 2 cm above terrain to dodge z-fighting, and reuses a single shared CanvasTexture across all airports (one GPU upload). Taxiing pilots get a runway-aligned visual cue for where the wind is blowing FROM without staring at the HUD wind readout; pairs naturally with the sock’s tilt/yaw animation so the whole assembly reads as one weather station.

- [x] Parking brake (`Shift+B`) — toggleable parking-brake latch that pins the wheels at low speed without holding B, so hold-short briefings stop being a wrist exercise. HUD shows a tiny amber `🅿 BRK` pill when set, a soft three-click ratchet plays on engage and a single softer pop on release. Plain B still hold-to-brake. Auto-releases above ~5 km/h ground speed (with a single softer click + status toast) so a forgotten brake can't sabotage takeoff, and resetPlane() + jump-to-airport both clear the latch. Help strip + panel updated.

- [x] Taxiway centerline lights — a row of small yellow emissive beads down the middle of the +x taxi strip at each airport, dusk-gated by the same sun-altitude window as the runway edge lights so they're dark at noon and glow at night. Visibility reuses the existing on-ground + 700m proximity gate the chevrons use, so air views stay clean. Beads thread a touch past either end of the runway so the row continues into the apron and up to the takeoff threshold for a fully guided ground path.

- [x] Pitot heat + icing at altitude (`Shift+I`) — when the plane climbs above 3000m AGL in rain or cold-weather TOD (sun low, ~19:00–07:00), the pitot tube slowly freezes over ~25s: indicated airspeed on the HUD (`#spd`) and the cockpit ASI needle lerps toward 0 while the underlying flight model (stall, AoA, audio) still uses true airspeed so behavior stays honest. Amber `⚠ PITOT ICE` pill on the SPD row lights once severity passes 0.15; ATC fires a one-shot "Pitot heat, pitot heat" the first frame ice latches past 0.5. Shift+I toggles pitot heat with a status toast — heat on melts ice in ~8s, off in clearing conditions thaws in ~16s. Pitot heat preference survives `resetPlane()` (it's a setting, not per-flight state) but pitot ice + ATC latch wipe on reset so a fresh sortie starts clear. Cockpit RPM gauge still reads true airspeed for the windmilling proxy so the tach doesn't lie when the ASI does.

- [x] Wingman challenge mode — new WINGMAN button on the intro screen. AI plane breaks out of its home-field circuit and flies a randomized 3-airport relay; the player has to stay within 200 m of it. HUD mission panel shows the next route point + a 10-segment `FORMATION LOCK` bar that drains at 0.2/sec outside the slot and refills at 0.12/sec inside (green > 50%, amber 20–50%, red below). Lead loiters straight ahead at 15 m/s while the player is still on the runway and the bar refills during that grace, then commits to the route at 38 m/s once the player rotates; a softer 35%-rate drain runs until the player joins for the first time so a slow climb-out can't instant-fail. Connecting-line glow reuses the existing formation tracker, and the legacy FORMATION! popup is suppressed in this mode so the lock bar is the single feedback channel. Bar to zero or player crash → mission fail with a LEAD/ATC scrub call; full route while still locked → MISSION COMPLETE banner + fanfare. Switching out of wingman drops the AI back onto the home-field circuit cleanly.

- [x] Pause menu screenshot thumbnail — when the player hits Esc to pause, the live WebGL canvas is grabbed via `toDataURL('image/jpeg', 0.55)` and stuck above the action buttons inside the pause card with a subtle `1px #2a4a6a` frame, soft drop-shadow, and a tiny gold `PAUSED` corner watermark. Capture runs *before* the overlay is shown so the thumbnail catches the live scene, and the data URL is cleared on Resume so the image doesn't pin RAM across many pauses. preserveDrawingBuffer is already on the renderer (photo mode relies on it), so no extra render is required. Pure cosmetic, fits in the existing 300px card without any layout jump.

- [x] Approach checklist popup — small right-side panel at `top:328 right:12` that fades in when within 4 km of any airport with gear up OR flaps still in AND descending (vs < -0.5 m/s). Four live-ticking rows (`GEAR DOWN`, `FLAPS`, `FUEL >10%`, `SPEED <200 km/h`); each glyph flips ☐→☑ and the row turns from grey to green the moment its condition is met. Carrier deck excluded (it's not a training field). Auto-hides on touchdown (`state.onGround`), outside the 4 km cone, or once gear+flaps are both set so it never nags in clean cruise. Hidden in pause/replay/photo/intro/crash and via PHOTO_HIDE_IDS so saved PNGs stay clean. Pairs naturally with the glideslope meatball, ILS cross-needles, and crab indicator — the whole right edge now reads like an actual training cockpit on final.

- [x] Pilot rank progression — lifetime rank derived from existing counters (CADET → STUDENT → PRIVATE → COMMERCIAL → ATP → INSTRUCTOR → ACE), each tier gated by ALL of: total flights, total landings, perfect-landing count (new counter, bumped when score ≥300), and Ring Chase best-time threshold so it isn't just a flight-spam meter. Main menu shows a `RANK <id> · NEXT <id> · <gaps>` pill under PILOT STATS/LOGBOOK; tapping it no longer launches the flight. Stats panel adds RANK + PERFECT LANDINGS rows, RESET also clears the rank latch so a fresh pilot bootstraps as CADET. A one-shot 🎖️ PROMOTION! toast fires the first time each rank threshold is crossed (latched in localStorage so reloads don't re-fire), checked on every takeoff / landing / ring best.

- [x] Cabin chime on level-off — the first frame autopilot altitude-hold captures within ±10 m of the locked target with vertical speed below 0.6 m/s, fire the iconic two-note seatbelt ding (1046 Hz → 784 Hz sine pair, 0.36s apart), pop a faint top-center `✈ CRUISING` ribbon for 2s, and queue an ATC "Cabin crew, prepare for cruise" line (PILOT tag). Latched so it only fires once per altitude lock; the latch clears on AP disengage, AP re-engage, resetPlane, and after the plane wanders >50 m vertically off lock (climb to a fresh altitude re-arms the chime). Suppressed in photo/replay/pause/intro and on the ground, and hidden via PHOTO_HIDE_IDS so saved PNGs stay clean. Reuses the existing sfxReady() context so no new nodes are allocated.

- [x] Ground vehicles at airports — one tug + one fuel truck per ground airport drifting around a tiny ~120m perimeter loop on the apron between the runway shoulder and the hangar front (10m × 50m rectangle centered at mid-runway). Tug is a low green box silhouette with cab + four wheels; fuel truck is a larger white hauler with a cylindrical tank on the bed, red accent stripes, and six wheels. Each carries a small amber emissive beacon on the roof that pulses on a ~0.85–0.95s sin² cycle (phased per vehicle so neither airport blinks in lockstep). Speeds (~4–6.7 m/s walking pace) and loop phase are staggered so the truck always sits half a lap behind the tug. Heading lerps along the loop path so each vehicle correctly faces its travel direction at every corner. Visibility gate mirrors the taxi chevrons — only shown when the player is on the ground OR low overhead (AGL <220 m) within 1.2 km of the nearest airport — so cruise air views stay clean. Pure scenery: no collision, no physics, no interaction. Pairs with the hangar + AI plane + windsock + beacon tower so the apron finally reads as populated.

- [x] Animated aileron / elevator / rudder surfaces — outboard ailerons on each wing TE, a single elevator slab on the tailplane TE, and a rudder slab on the fin TE all lerp toward the live effective stick / autopilot / trim input (~150ms ease) and pivot around their leading-edge hinges. Roll command produces mirrored deflection on the two ailerons, pitch deflects the elevator TE up/down, yaw swings the rudder TE left/right. Clamped to ~22° aileron/elevator and ~28° rudder so the slabs never clip the surrounding airframe. Visible in every external cam (chase, flyby, orbit, replay, photo); hidden in cockpit cam by the existing airframe-hide loop. Surfaces snap back to neutral on resetPlane / quick-jump teleport.

- [x] Aircraft nav + strobe lights — solid red wingtip light on the left, solid green on the right, solid white on the tail, plus a pair of bright white wingtip strobes that double-flash every ~1.4s. Gated to the same dusk window as the runway edge lights so they're off at noon and dazzling at night, with the strobes punching a quick additive glare sprite so they read from 6 km out. Hidden on the player plane in cockpit cam (caught automatically by the existing plane.children airframe-hide loop because the bulbs/strobes are direct children of the player group) but always on for the AI plane, so traffic suddenly looks like real traffic after sunset.

- [x] Go-around toast + ATC call — missed-approach detector inside the existing radalt callout block: when the player slams throttle past 95% below 300m AGL within 2 km of a runway with positive vertical speed and gear still down, fire a one-shot "🛬 GO-AROUND" toast + PILOT ATC line "Going around, traffic acknowledged." Re-arms once AGL climbs back above 200m (mirrors the radalt arm/re-arm pattern). Gated to free-flight + touch-and-go so scored hop/ring/carrier/wing missions never pop it on a botched final. Missed approaches finally feel like a real decision instead of just "engine got loud again".

- [x] Rain droplets on the windshield — cockpit-cam-only wet-glass overlay that runs when settings.rain is on. A pool of ~22 small white-blue droplets sits in the lower two-thirds of the screen with soft radial-gradient heads + trailing streaks, drifting down-and-back, fading in at the top and out near the bottom, raked sideways by airspeed (faster IAS = harder tilt) with a mild crosswind nudge. Layers above the existing #rainOverlay streaks so the glass actually reads as wet from inside the cockpit. Reuses the rain settings toggle so there's zero new UI; hidden in photo / pause / replay / intro via the same gating pattern as the bird-strike splatter so saved PNGs and frozen frames stay clean.

- [x] First-flight tutorial overlay — on the very first launch (latched in localStorage as `flightsim.tutorial.v1`), a centered welcome card pops above the intro menu listing the core controls in two columns (FLY THE PLANE: Throttle / Pitch / Roll / Yaw; USE THE COCKPIT: C cam / H autopilot / G gear / F flaps / B brakes / X engine / I ignite) plus a one-line tip about rotation speed and where to find settings. Big GOT IT primary button + an in-card "Don't show again" checkbox (defaults checked so the GOT IT path silences future launches; unchecking it leaves the card armed for next session). Backdrop click and Esc dismiss the same way, and Space/Enter is blocked while the tutorial is on screen so the player can't accidentally launch the sim into an unfamiliar cockpit. Settings panel now has a TUTORIAL row with a button that toggles the latch — "SHOW TUTORIAL NEXT LAUNCH" when seen (clears the latch so the card re-pops on the next page load), "TUTORIAL ARMED (CLICK TO CANCEL)" when already armed. New players finally get a roadmap of the keymap before they crash three times figuring out what `I` does.

- [x] Engine start checklist toast — first cold-start `I` press now fires a 3-line `📋 PREFLIGHT — FUEL ON ✓ / MIXTURE RICH ✓ / MAGS BOTH ✓` toast that fades over ~2.5s while the starter is spooling. Pure flavor over the existing cold-start ritual — no physics, no extra gating — just gated to `state.coldStart` (skips in-air deadstick restarts) and hidden in photo / replay / pause via the same pattern as the bird-strike splatter so saved PNGs and frozen frames stay clean. Reuses the achievement DOM host + base style with a tighter `achFade` delay so the card clears before the engine actually catches at `COLD_START_SPOOL_S`.

- [x] Compass card click-to-bug — clicking anywhere on the compass strip at the top of the HUD now drops the heading bug onto the bearing under the click (snapping to the nearest degree). CSS-px → internal-canvas-px scale matches `drawCompass`'s 6 px/deg so the bug lands exactly under the cursor; AP heading-hold picks up the new bug on the same frame and waypoint-follow auto-disengages so the pilot is back in manual aim. Gated off intro/replay/photo for the same reasons as the keyboard nudges; `touch-action: manipulation` makes phone taps register cleanly without a 300ms zoom delay.

- [x] Runway numbers painted on the threshold — each runway now wears its two-digit ICAO designator (`36`/`18` at KSEA, `02`/`20` at KHIL, `09`/`27` at KLAK) in big white block letters at both ends, set ~30 m inboard of the piano-key threshold stripes. Built in `makeAirport` via a small `paintRunwayNumbers` IIFE that calls the existing `runwayDesignator()` helper (so painted numbers always match the ATC/airborne/approach callouts), rasterizes a 150 px monospaced glyph onto a 128×192 canvas texture, and lays it flat on a `PlaneGeometry(14×18)` 0.045 m above the runway with `polygonOffset`. The `+z` end paints `runwayDesignator(a.hdg)` (the heading you fly when landing into that threshold) and reads upright by default; the `-z` end paints `runwayDesignator(a.hdg + π)` and yaws 180° around the runway-up axis so it also reads upright to a pilot approaching from the opposite side. Carrier deck keeps its existing `01` lettering. Free immersion upgrade on every approach — "land on 27" finally means something visually.

- [x] Map clip-to-airport on the mini-map — double-clicking an airport icon on the mini-map now snaps the magenta waypoint pin straight onto that airport's centre (works for the rim-tick when the field is past the visible range too, so distant airports are one dblclick away). New `dblclick` handler on `minimapCv` recomputes each airport's drawn screen position with the same `cx + (a.x - plane.x) * scale` math `drawMinimap` uses, picks the nearest one within a 14 canvas-px tolerance, and force-sets `state.waypoint = { x: ap.x, z: ap.z }`. Two normal clicks still fire underneath (set-near + toggle-clear), so the dblclick runs last and the net result is always "pin is on the chosen field"; waypoint-follow auto-disengages so the pilot can pick `M` to re-arm AP onto the new target. Gated by the same `canSetWaypoint()` (free flight, not intro/replay/photo/pause/crashed) the click path already uses, fires the existing `showStatus`/`pushATC` lines (`WAYPOINT → <id>` + `Waypoint set to <id>, X.X km.`), and reuses the entire pin/bearing/distance/AP plumbing so no new HUD or physics surface area.

- [x] ATC line on takeoff clearance — first frame the player advances throttle past 70% on a runway with brakes off, tower fires a one-shot `"<id>, cleared for takeoff RWY <DD>, wind <DDD> at <NN>."` (or `"winds calm"` under 3 kt). Runway designator comes from the existing `runwayDesignator()` helper so reciprocal departures off the same strip read 09 vs 27 correctly, and wind direction/speed reuse the same `atan2(-wx,-wz)` + 1.94384 conversion the existing `_windCall()` ATC helper uses, so the call format matches the rest of the strip. Latched per departure so a long static run-up at full throttle doesn't spam, and re-arms once ground speed drops below 0.8 m/s on the ground so back-taxi + go and touch-and-go full-stop patterns each get a fresh clearance. `resetPlane()` also re-arms so a hot reset on the apron gets the call. Cold-start ritual is unaffected — throttle is pinned at 0 until ignition, so the call only ever fires when the engine is actually running. Closes the takeoff loop alongside the existing aileron-into-wind coach and the airborne callout.

- [x] Engine torque effect on takeoff — single-engine P-factor / spiraling-slipstream now pulls the nose left on full-throttle takeoff rolls. New `torqueBias` term in the on-ground yaw-rate path scales `-0.13 * throttle² * speedFactor`, where speedFactor lerps 1→0 between brake-release and ~80 km/h ground speed (lift-off) so the tug is strongest at the start of the roll and naturally fades to nothing by rotation. Negative sign = left swing, big enough to walk the nose off centerline if ignored over a 30s roll but small enough that ~0.15 right rudder (yIn≈+0.17) cleanly cancels it. Reverse thrust on rollout flips the sign so the nose pulls the opposite way during a hard reverse — same effect a real prop reverser exhibits. Gated to `engineLiveForRev` (engine running) and `state.throttle > 0.05` so an idle taxi taxis straight, and skipped entirely when `settings.twinEngine` is on (counter-rotating-style symmetric thrust readout cancels P-factor). Pure flight-model tweak — no new HUD, no new audio — but every takeoff is now a real-pilot "feed in right rudder" moment and pairs naturally with the recent takeoff-clearance ATC line.

- [x] Squawk code on the HUD — small `SQ 1200` pill under the right edge of the ATC strip showing the current 4-digit transponder code. VFR default `1200` for free / TnG / ring / carrier / wingman, hop missions roll a fresh random 4-digit code per leg (avoiding the reserved 7500/7600/7700/1200/0000), and any active emergency (engine fire, fuel ≤10%, or airframe damage past 80%) latches the code to `7700`, paints the digits red, and fires a PILOT "Squawk 7700, declaring emergency" call. Latch clears once every emergency condition resolves and the pre-emergency code (incl. the active hop leg's assigned code) comes back, so a brief scare doesn't permanently nuke the player's assignment. Every code change pulses an amber border highlight for 1.4s so the player notices the swap. Hidden in photo mode.

- [x] Brake-temperature gauge — eighth small canvas in the cockpit panel showing brake disc temperature (°C). Climbs while `B` is held against ground roll (scaled by speed × brake force × dt), bleeds back to ambient at ~3°C/sec free-rolling and ~8°C/sec when stopped on the apron. Green band 30–180°C, red redline past 320°C. Past the redline, brake friction drops 50% for the next 6s (latched + one-shot ATC "Brakes overheated, cool before next stop" call + red FADE tag under the gauge digits) so dragging brakes down a long rollout has a real consequence. Cools naturally with the existing apron-refuel hang time, so the loop self-balances.

- [x] Touch-and-go counter on the HUD — small `T&G ×N` streak row added to the existing TOUCH & GO score panel (hidden until streak ≥ 2 so it doesn't shout on the first land of a session). Every good runway touchdown that follows a prior touchdown within 12s — and only counts after a lift-off, so you can't double-tick a single bounce — increments the streak. Full-stop landings (rollout latch fires below ~0.8 m/s ground speed) and crashes both reset it to 0; resetPlane, the J quick-jump, and a fresh mission also wipe it. Lifetime peak goes to a new `BEST T&G STREAK` row in the PILOT STATS panel, persisted to localStorage via the existing `pilotStats` save path. Pure scorekeeping — no new physics, no new audio — that finally rewards the touch-and-go practice loop.

- [x] Distance-to-runway readout on the HUD — new `RWY` row added right under ALT in the main HUD panel showing straight-line distance to the nearest runway threshold (either end of any runway, including the carrier deck since it lives in `runwayInfos`). Reads as metres under 1 km (e.g. `820 m`) and kilometres above (`1.4 km`). A small light-blue arrow next to the number rotates to point toward that threshold relative to the plane's current heading (up = dead ahead, right = 3 o'clock, etc.). Hidden on the ground and beyond 5 km so it never shouts in cruise; auto-hides in photo mode via the existing `#hud` PHOTO_HIDE_IDS sweep. Pure derived readout off the existing runway-info plumbing — no new physics, no new audio — turns the mini-map's eyeballed distance into a hard number pilots can call out on downwind to base.

- [x] Landing light on the airframe (`L` to toggle) — left-wing-root forward-facing SpotLight (no shadows, 220m range, ~16° half-angle) plus a faint additive fff4c8 ConeGeometry beam so the bulb reads in mid-air, not just on the ground. Intensity eases on a 0..1 visibility lerp (4/sec) and scales day-soft (0.20) ↔ night-bright (1.85) via the existing `sunDirFor(state.todHours)` sun curve, with an extra +0.55 punch when rain is on so the wet runway actually lights up. `L` (and the new 💡 LIGHT mobile button next to IGN/REV/BRK/CAM) toggles `state.landingLight`, persists `settings.landingLight` to localStorage, and shows/hides a `💡 LIGHT` HUD pill next to the REV pill. Auto-suggest: first time AGL crosses below 200m inbound to a runway on final at night (TOD < 6 or > 19), fires a one-shot pilot ATC "Landing lights on, runway in sight." + status banner; latched per flight via `state.landingLightSuggested`, cleared in `resetPlane()`. Hidden in photo / pause / replay / intro so saved PNGs stay clean. Help strip and `preventDefault` keylist updated.

- [x] Gear warning horn — when AGL < 80m, gear up, throttle < 30%, and descending, a two-tone square-wave "gear, gear" pulse fires every ~0.8s (520Hz → 390Hz, layered through the existing WebAudio path) alongside a blinking amber `⚠ GEAR UP` pill stacked under the FLARE row in the warnings column. Silences instantly when the gear drops or the player slams throttle past 50% (go-around). Re-arms above 200m AGL (so a touch-and-go gets a fresh nag on the next pattern) or on the ground (so a full-stop wipes the latches). One-shot ATC `"Check gear down."` (TWR) the first frame the warning latches per arming so the call doesn't spam the strip. New `tickGearHorn()` runs alongside `tickWindShear()` in the per-frame update; reuses the existing nearest-airport AGL math so the trigger fires even when not strictly aligned with a runway (a low pass over the home field with gear up still nags). State + latches reset in `resetPlane()`, hidden in crash / replay / photo / pause / intro via the same gate the other warning pills use.

- [x] Animated windshield wipers (`N` key, or AUTO with rain) — new `#wipers` 2D canvas overlay layered just above `#windshieldRain` and only shown in cockpit cam draws two black wiper arms pivoted near the bottom of the screen sweeping in a synchronized fan via a `sin(phase)` sweep. Modes are `off` / `slow` (1.0 Hz) / `fast` (1.8 Hz) / `auto` (1.0 Hz whenever rain is on), persisted in `settings.wipers` (default AUTO) and exposed in a new WIPERS row in the settings panel. `N` cycles off → slow → fast → off (W was already throttle, so the cycle moved to N and is documented in the help strip + status toast). Each direction change fires a short bandpassed-noise `thwip` through the shared sfx context (volume-scaled, 120 ms debounce). Pivots, arms, blade rubber, and tiny mount hubs are all canvas primitives so the implementation is mesh-free and scales with window resize via `sizeWiperCanvas()`. Hidden in chase / cockpit-off cams, photo, pause, replay, intro, and added to `PHOTO_HIDE_IDS` so saved PNGs stay clean.

- [x] Pitot heat toggle (`U` key) — the existing pitot-icing model already builds ice at altitude in rain / cold TOD; this ship adds a dedicated `U` hotkey that toggles `state.pitotHeatOn`, persists the choice to `settings.pitotHeat` (default OFF), and seeds the runtime state from the saved value so the toggle survives reloads. SPD HUD now flips to red with a small `ASI` tag the frame indicated airspeed lies more than 10 km/h below true (stall red still wins). A `🔥 PITOT` amber pill lights on the WIND row whenever heat is on, and the cockpit panel grows a small amber `● PITOT HEAT` lamp in the top-right corner for pure flavor. ATC ice-latch line shortened to "Airspeed unreliable, check pitot heat" so the call matches the spec, and the legacy Shift+I binding was retired (help strip + comments updated). All pills live inside `#hud` / `#cockpitPanel` so the existing photo/pause/replay/intro/crash hide list keeps them clean.

- [x] Carrier steam catapult launch (`O` key) — hold `O` while parked stationary (<3 km/h ground speed) on the carrier deck inside the touchdown box and pointed within ±20° of the bow axis to charge the cat. New `catapultEligible()` gates on `state.onGround` + `carrierDeckHeight()` + box `lz` + ground speed + a dot-product heading check against the deck's bow axis (`sin/cos(CARRIER.hdg)`) so eligibility doesn't depend on the HUD compass convention. Charging is `state.catArmT += dt` up to `CAT_ARM_S=1.0`; releasing or losing eligibility decays it at 2x so a brief slip doesn't strand a quarter-charged shuttle. Hitting arm flips `state.catFireT` from -1 to 0, fires the SFX (one-shot launch-bar triangle clunk + bandpassed-noise rising whoosh + sub-sine rumble through `sfxReady()`), pushes a PILOT "Cat firing, hang on." ATC line, and emits the first steam puff. Firing branch ticks `catFireT` for `CAT_FIRE_S=2.0`s, overrides `state.vel` horizontal to `fwd * lerp(0 → CAT_TARGET_KMH/3.6)` each frame (engine throttle left to the pilot so they still own climb power off the bow) and emits a steam puff every ~0.12s through a new 8-slot `Sprite` pool that mirrors the reverser-dust pool (`airshowTex`, white tint, drifts up + grows + fades). On completion fires a TWR `"Cat shot, cleared for takeoff."` and clears state. HUD pill `⚡ CAT ARMED X.Xs` (amber) during charge, `▶ CAT FIRING X.Xs` (green) during the run, hidden in photo mode + when idle. Help strip + `preventDefault` list updated with `O`; `K` was already pitch-down, so `O` won (no conflict) instead of overloading `L` (landing light) or `K`. Reset wiped in `resetPlane()` with a TDZ try/catch on `clearCatSteam()` since the pool spawns after the first reset call. Pairs with the existing carrier deck + arrestor friction so the ship finally feels like a working cat-shot launch pad, not just a landing strip.

- [x] Real-time fuel flow gauge (GPH) — ninth small canvas added to the cockpit panel (alongside CHT, oil, brake temp, compass, etc.) showing live gallons-per-hour drawn by the engine. Reads ~2 GPH at idle, climbs along a mild quadratic curve through ~9 GPH at cruise (60% throttle), and hits ~16 GPH at full power; scales by engine state (0 GPH on kill / fuel-out, mid-spool ramp on cold-start and air-restart). Green cruise arc 4–10 GPH, red over-burn arc 14–20 GPH so prolonged full-throttle climbs visually scream "you're burning the tank." Uses the same canvas2d bezel/needle helpers as the existing gauges so the panel reads as a matched set, and the readout color matches the RPM gauge rules (grey when the engine isn't burning anything, red past the over-burn band, green when sipping fuel like a sane cruise).

- [x] Canopy open animation (`\` key) — visible cockpit dome lives on a rear-edge hinge group that lerps ~70° backward over 0.6s when `\` is pressed (bound to backslash because `O` is already catapult). Ground rule: throttle must be at idle to toggle, else a `CANOPY LOCKED` nudge fires. Air rule: opens are allowed but bump parasite drag (+8% scaled by anim) so cruise speed visibly drops, with a looping low-pass-filtered noise wind-rush that fades in past 80 km/h and pegs near 260 km/h. Engine start (cold-start `I` or in-air deadstick restart) is now blocked while the canopy is open and fires a one-shot ATC `"Close the canopy before starting engine."`. Opening in flight fires `"Canopy unsecured, request immediate landing."` once per pop (re-arms after closing + a full-stop). HUD shows a `🔓 CANOPY` pill in the warnings column whenever the canopy is anywhere off-closed; hidden in cockpit cam (we're inside it) via the existing plane.children airframe-hide sweep and in photo mode via the `#hud` PHOTO_HIDE_IDS entry. Closed-state silhouette is bit-identical to the legacy dome (same hemisphere geometry + scale, just parented). Snaps closed and silent on resetPlane so every fresh sortie starts ready to crank.

- [x] Aerobatic competition box — a 1km × 1km × 800m invisible volume centered on MAIN_AP. Entering the box in **free flight** with gear up, throttle >40%, AGL between 100–900m arms a 60s run: a magenta `🎯 AEROBATIC BOX` panel pops top-center under the squawk strip with a live countdown and live score. Existing acro detectors do the heavy lifting — loop completion = +100, aileron roll = +75, a loop+roll (either order) within a 4s pair window credits an extra +50 "HALF-CUBAN!" toast, and every chained maneuver after the first adds +25 (hint line shows the live chain count). Leaving the box, dropping gear, throttling below 40%, going AGL <100m, or letting the timer expire ends the run; positive scores persist `settings.aerobaticBest` (validated on load) and toast the final number. Bronze/Silver/Gold achievements at 250/500/1000 fire once each per run via the existing `unlockAchievement` set. Mini-map draws a magenta dashed 1 km square centered on MAIN_AP when within 2 km of the footprint (brighter outline + thicker line while a run is active so "I'm still inside" reads at a glance). Hidden in photo mode via PHOTO_HIDE_IDS.

- [x] PAPI lights at each runway threshold — a physical four-light bar on a low post sits on the left side of every ground-runway threshold (6 bars across KSEA / KHIL / KLAK, 30 m inboard, 6 m off the edge stripe). Each bulb is calibrated to its own slice of the 3° glideslope (2.50° / 2.83° / 3.17° / 3.50°, inner→outer), so the live pattern reads ALL RED below ~2.5°, ALL WHITE above ~3.5°, and the classic 2-RED / 2-WHITE exactly on the standard slope. `tickPapiLights()` shares the `runwayInfos` aim point used by the HUD meatball + ILS needles so the lights are a pure visual mirror of what the HUD already says, but readable from the actual approach window when the player isn't staring at the HUD. Dusk-gated via the same `sunDirFor()` ramp as the runway edge lights so the bars punch through at night and stay subtle in midday, and the per-bulb update freezes within 50 m of the aim point so the pattern doesn't flicker as the plane crosses overhead at touchdown. Carrier deck stays opt-out by design (no fixed threshold post on a moving deck — the existing meatball handles approaches there).

- [x] G-force vision effects (greyout / redout) — sustained positive Gs above ~5G fade a black radial vignette in from the screen edges (greyout), peaking near full tunnel vision around 8G, and prolonged negative Gs below -2G fade a soft red tint over the frame (redout). Vignette/tint ease in over ~1.5s and decay over ~2s once Gs return to normal so a brief snap pull doesn't dim the world. Computed off a new `state.gSigned` (unclamped lift-derived G, so negative-G pushovers actually count) — `state.gLoad` and the existing G-meter HUD readout are unchanged. Asymmetric ease keeps the tunnel lingering a beat after you unload so the recovery is felt. Display flips off entirely when both opacities settle to zero, and the effect is hard-gated to live flight (hidden in photo / pause / replay / intro / crash / on-ground via the standard checklist) so saved PNGs stay clean and the cold-start ritual doesn't carry a stale tint. Pairs with the existing G-meter + crash card so players learn to respect sustained pulls before snapping a wing or blacking themselves into a crash, and gives aerobatic competition runs a real physiological ceiling.

- [x] Fog weather toggle in settings — new FOG row in the settings panel (OFF / LIGHT / HEAVY) clamps `scene.fog.near/far` to ~600/3000 m on LIGHT and ~200/1000 m on HEAVY, so the world dissolves into a soft grey wall well before the camera's far plane. Fog *color* still tracks the time-of-day key (and the rain mood lerp) inside `applyTimeOfDay()`, so dawn fog reads warm, midnight fog reads near-black, and rain still pushes everything overcast. HUD adds a blue `🌫 FOG` pill in the WIND row whenever LIGHT/HEAVY is active (hidden in photo mode so saved PNGs stay clean). Persisted to localStorage so the player can chain hardcore IFR approaches across sessions. Pure renderer knob — no flight model side-effects — so a HEAVY fog approach forces a real scan on the existing ILS cross-needles + glideslope meatball + crab indicator to find the strip.

- [x] Carb heat toggle (`Shift+C`) — toggleable carb-heat control that mirrors the existing pitot-heat hotkey pattern. When throttle is below 40% in cold / damp conditions (rain on, or TOD before 7 / after 19), the engine starts building a small carb-ice fraction that bleeds available thrust by up to ~15% over ~25s of accumulation; pulling carb heat with `Shift+C` immediately starts melting the ice back at ~3x the build rate while also costing a flat ~3% thrust penalty (denser intake air). HUD shows a `🔥 CARB` amber pill in the WIND row when heat is on (plus a `⚠ CARB ICE` yellow pill when ice is building), and the cockpit RPM gauge dips ~80 RPM the frame heat is pulled (real-world tell). One-shot ATC "Carb ice — apply heat" call the first frame ice crosses 25% per session, latched. Persisted to localStorage, default OFF. Tiny new physics surface that gives the throttle-low cruise a real systems decision to make.


- [x] Parachute bailout (`Shift+P`) — when airframe damage is past 65% OR an engine fire is active OR fuel is exhausted OR the plane is inverted nose-down below 250m AGL, a cyan `🪂 BAIL` pill lights on the HUD's WIND row and `Shift+P` triggers a parallel mini-sim: a tiny pilot mesh (helmet + flight suit + stub arms/legs) ejects up + back from the cockpit with the plane's current velocity inherited, gravity-drops for ~0.85s with a wind-rush whoosh SFX, then a hemispherical international-orange canopy with shroud lines deploys and the pilot drifts down at ~4.5 m/s, blending toward the live wind vector so the chute visibly drifts. Camera retargets to a chase position ~10 m off the pilot pointed slightly downwind so the canopy is always framed against sky. Abandoned plane keeps its physics and crashes wherever it was going (no respawn cheese), and the regular crash card is suppressed during the bailout so the cyan bailcard pops solo on touchdown with eject altitude AGL, horizontal drift, descent time, and a lifetime `BAILOUTS` counter that also shows up in the pilot stats panel. Logbook verdict reads `BAILED OUT — PILOT SAFE` instead of `CRASHED`.

- [x] Ground crew marshaller at the apron — small static figure (orange vest + raised wands) stands by the apron taxi-chevron at each airport. When the player taxis within 25 m at <8 km/h, the marshaller's wands animate (lerp between two poses at ~1.2 Hz so it reads as "come forward, slowly") and a brief ATC `"Marshaller in sight, follow signals."` line fires once per arrival. Pure scenery + immersion bump; pairs with the refuel-on-apron loop so the apron feels alive instead of being an empty patch of asphalt. Hidden in photo mode.

- [x] Photo countdown (`Shift+S` in photo mode) — in photo mode, `Shift+S` starts a 3-2-1 visible countdown (big centered green digits with a pop-in animation) before grabbing the PNG. Each tick fires a soft sine chime (final tick pitched a fifth higher as the shutter cue) so the count reads visually and audibly. Plain `S` still saves immediately (and cancels any active countdown); Shift+click on the SAVE button mirrors the keybind for mobile/mouse users. Countdown drains via dt in the existing photo-mode loop branch so the rest of the sim stays frozen, and the digit overlay is hidden in `savePhoto()`'s overlay-hide sweep so it never bakes into the screenshot. Exiting photo mode (P / Esc) cancels any in-progress countdown.

- [x] Squawk code keypad (`K`) — small 4-digit keypad overlay (tap numbers or type 0–7) for setting the transponder squawk shown next to the ATC strip; presets for 1200 (VFR), 7500 (hijack), 7600 (radio fail), 7700 (emergency). Each preset fires a matching one-shot ATC line. K toggles open/close, Backspace erases, Enter applies (also auto-applies on the fourth digit), Esc cancels. Gated against intro/replay/photo/pause/crash and the J overlay; manual codes survive an active 7700 emergency latch via the existing pre-emergency restore path. Clears `keys['KeyK']` on open/close so K still works as pitch when the keypad is closed. Hidden in photo mode via `PHOTO_HIDE_IDS`.

- [x] Engine warning chime test (`Shift+T` on the ground) — holding `Shift+T` on the ground with the parking brake set cycles through every cockpit warning sound in sequence (stall horn, gear horn, GPWS pull-up, fire chirp, oil pressure low) at ~1.1–1.2s each, each step popping a `TESTING: <name> <step>/5` toast so the pilot can verify audio settings before takeoff. Stall burst rolls a one-shot pulsed 880 Hz square so it doesn't fight the live `setStallAudio()` in `updateHud()`; gear / GPWS / fire chirp reuse the existing one-shot helpers; oil pressure gets a new 290→225 Hz triangle warble (low pressure currently only triggers an ATC line, so the test needed a dedicated sample). Release Shift or T to abort mid-cycle; finishing the sequence shows `WARNING TEST · all chimes verified`. Gated to ground + parking brake + non-intro/photo/replay/pause/crash and against the J / squawk modals; airborne presses are silently ignored, and trying without the brake set pops a `set parking brake first` hint. Pairs naturally with the cold-start ritual as a pre-flight checklist beat.

- [x] Pre-flight checklist overlay (`Shift+K`) — compact left-side panel under the HUD with the six items a real pilot scans before takeoff: CANOPY (closed), PARKING BRAKE (set), FUEL (≥25%), TRIM (neutral within ±5%), FLAPS (any takeoff detent), PITOT / MIXTURE (pitot heat on + fuel > 5% + engine alive). Each row auto-ticks green with a soft sine pip the first time it goes OK during a session, and shows a live state pill (`OPEN`/`CLOSED`, `SET`/`OFF`, `83%`, `NEUTRAL`/`NOSE-UP 12%`, `STAGE 1`, `PITOT OFF`/`MIXTURE LEAN`/`READY`); pending rows stay amber. Footer reads `n / 6 complete` until you hit `✓ READY FOR TAKEOFF` in green. Shift+K toggles; the panel auto-closes on liftoff (and on photo / replay / intro / pause / crash / leaving the ground) so it never lingers into a flight. Pure HUD overlay — doesn't pause the sim and stays out of `isAnyOtherModalOpen` so Esc still goes to the pause menu cleanly. Shares no state with the existing in-flight `#checklist` (approach card on the right); the squawk `K` handler now also guards on `e.shiftKey` so Shift+K never opens the transponder by accident. Closes the pre-flight beat alongside the cold-start ritual and the chime self-test.

- [x] Fuel range ring on the mini-map — dashed circle around the plane on the mini-map showing remaining straight-line range at the current GPH burn (radius scales with `fuelL / (gph * 3.785) * cruiseKmh / 3.6`, expressed in game units by mirroring the cockpit FUEL-FLOW gauge curve and a ~210 km/h cruise speed). Cyan when the range is at least double the distance to the nearest field, amber when range > distance but the cushion is thin, red when you literally can't make it. Drops back to nothing when the engine is dead and the tank is dry (the deadstick glide ring is a different problem), capped to the visible map radius so a fresh-tank-at-idle ring doesn't ask canvas for a giant arc, and skipped in photo mode for the same reason the rest of the minimap overlay is. Pairs with the new fuel-flow gauge so the cockpit number turns into a spatial "can I make it?" answer at a glance.

- [x] ATIS info loop (`Shift+A`) — `Shift+A` near an airport (within 8 km) synthesizes the current automated weather and reads it through the ATC strip with a TWR-style `ATIS` tag: "<id> information ALFA, winds <DDD> at <NN>, altimeter <three zero point zero six>, runway <DD> in use, temperature <TT>, time <HHMM> Zulu." Digits are spelled out ("three zero point zero six") so the line reads like a real ATIS read. Wind / heading / time-of-day come straight from the live HUD plumbing (`_windCall()` math, `runwayDesignator()`, `state.todHours`) so the broadcast can never drift from what the rest of the cockpit shows. Active runway picks the wind-favoured threshold by projecting the wind vector against both runway directions and selecting the smaller tailwind component (falls back to canonical heading under 1.5 m/s wind so the call doesn't flip-flop in calm air). Altimeter is a stable per-session value in 29.92–30.20 inHg, the info ID (ALFA, BRAVO, …) rotates only on page load so re-broadcasts within a session match, and temperature derives from a smooth diurnal cosine (~7°C dawn, ~24°C mid-afternoon, −3°C if rain is on). A `📻 ATIS · <id> ALFA` status pill flashes with the wind/runway/altimeter readout, two short sine pips (G5 → C6 through `sfxReady()`) cue the start of the call, and a 4.5s cooldown swallows held-Shift+A so a fat-fingered double-tap doesn't fire-spam the strip. Gated against intro/replay/photo/pause/crash, `KeyA` is cleared from the keys map on the press so the player doesn't get a stray roll-left input on the same frame, and `resetPlane()` clears the cooldown so a hot reset can grab a fresh weather read immediately.

- [x] Random emergency landing site finder (`Shift+E`) — when engine is dead or on fire and the plane is above 200m AGL, `Shift+E` pings `findEmergencyLandingSite()` which scores every real airport (KSEA / KHIL / KLAK), the carrier deck CV-01, and a 7×7 grid of terrain samples in a 1–3 km annulus around the plane (`emerFieldFlatness()` rejects ocean/lake/deck and any patch where 4 cardinal ±30 m neighbours spread more than 4 m or exceed a ~4° slope). Picks the closest reachable inside a 9:1 still-air best-glide cone, with reachable runway/carrier surfaces beating reachable open fields and any in-glide pick beating out-of-glide candidates so the picker never silently steers the pilot into a worse option. Drops the result into `state.emerSite = { x, z, elev, id, kind }`, fires a `MAYDAY, divert to <id>, <D> km` PILOT ATC call, and toasts `EMERGENCY SITE` / `BEYOND GLIDE` with the distance + altitude margin. A new `EMR` HUD row shows bearing/distance/altitude margin live (magenta when reachable, red when the pilot has glided below the 9:1 cone) and `drawMinimap` paints a pulsing magenta diamond + halo at the site or an off-rim chevron pointing at it when the field is beyond the map. Lifecycle auto-clears on engine restart, touchdown, crash, `resetPlane()`, or once the plane glides within 250 m of the site. Gated against intro / replay / photo / pause, and `KeyE` (yaw right) is cleared from the keys map on the shifted press so the finder never injects a stray yaw input.

- [x] Taxiway signs at intersections — one sign cluster on a 1.1 m grey post at each runway threshold on the +x taxi-strip side of every ground airport (KSEA / KHIL / KLAK). Each cluster carries a red mandatory-instruction `<DD>-<DD>` runway-holding panel (`36-18` at KSEA, `02-20` at KHIL, `09-27` at KLAK — designators come from the existing `runwayDesignator()` helper so the painted text always matches the runway-number paint and the ATC callouts) above a black-on-yellow `A1` / `A2` taxiway location panel below it. Panels are 2.2×0.8 m two-sided `PlaneGeometry` with canvas-texture letters rendered through a single `makePanelTexture(label, bg, fg, border)` helper so both colors share one code path, white-on-red and black-on-yellow inset borders match real FAA signage, and the post sits just off the runway edge (`a.w/2 + 5`) inboard of the chevron row so a taxiing pilot meets the hold-short sign before crossing onto the runway. Pairs naturally with the existing taxi guidance arrows, the marshaller, and the taxiway centerline lights so the apron-to-runway taxi finally reads like a real airport instead of an unmarked strip of asphalt.

- [x] Plane shadow on the ground — soft dark elliptical disc painted on the terrain directly below the airframe, sharp + dense when low (~0.65 opacity on the runway), fading + growing with altitude, and culling past 600 m AGL so high-altitude flights pay nothing. Disc tracks `plane.position.x/z` at live `terrainHeight(x,z)` so it walks correctly across runway, apron, grass, and the carrier deck; `polygonOffset` keeps it from z-fighting any of those. Geometry stays a unit `PlaneGeometry` rotated flat — size is driven by `scale.set(sx, sz, 1)` so a single mesh handles both the altitude growth (~1.0x grounded → ~2.4x at the cap) AND the silhouette stretch from bank/pitch (`1/cos(roll)` along the span, `1/cos(pitch)` along the fuselage, clamped so a knife-edge can't blow it up). Shadow yaws with `plane.rotation.y` so the elliptical footprint always reads as "airframe straight down". Skipped in intro (decorative world) and replay flyby (old camera poses) so it never pokes through; left on in photo mode because real shadows are exactly what photographers want. Cheap: one `setPosition` + one `setScale` + an opacity write per frame.

- [x] Tower view camera — added `TOWER` as a 5th C-key camera mode after chase / cockpit / flyby / orbit. Camera pins to the nearest airport's beacon-tower position at 12 m above the ground (reuses the exact `(a.w/2 + 9, a.len/2 - 18)` local offset that `makeAirportBeacon` uses, rotated into world space by the airport's heading, so the camera sits at the same landmark the player already sees from the air) and looks at the plane each frame so the controller-cam pans naturally as the plane moves through the pattern. Iterates `AIRPORTS` with the same `Math.hypot(plane.position.x - a.x, plane.position.z - a.z)` math the `_nearestApId()` ATIS picker uses; if no airport is within 10 km the cam falls back to the chase pose so it never strands the camera in empty terrain. `CAM_NAMES` grew the `TOWER` entry, the C-key handler and the mobile cam button both switched from `% 4` to `% CAM_NAMES.length` so adding more modes later is one-line, and the HUD pill auto-flashes `CAM: TOWER` via the existing `updateCamModeUi()` path. No new geometry, no new state — pairs perfectly with the existing beacon, light-gun signals, and ATC ticker so you can finally watch your own pattern from the controller's seat.

- [x] Trip / Hobbs timer in HUD (`Shift+M` to reset) — small `TRIP HH:MM:SS` chronometer pill pinned just below the squawk strip (top:114px, same `translate(176px,0)` offset on desktop; reflows to the right edge on mobile via the same `@media (max-width: 760px)` rule the squawk pill uses). New `tickHobbs(dt)` runs alongside `tickSquawk()` in the main loop's `!introOpen && !photoMode && !paused` gate and additionally bails out under `state.replayActive` so a post-crash playback doesn't keep adding to a frozen flight's time. Auto-arms `state.tripRunning = true` the first frame `state.vel.length() >= 5` m/s, freezes back to `false` on full-stop (`< 0.5 m/s` while `onGround`) or `state.crashed` — pause/menu freeze is free since `step()` isn't running there. Each tick accumulates `state.tripSec += dt` and only writes `hobbsValEl.textContent` when the displayed integer second actually rolls over (a `_hobbsLastShownSec` latch), so the per-frame cost is a couple of guards + a `state.vel.length()` call. While running the pill takes a green `.run` class so the digits glow `#7be39a` and the pilot sees at a glance that the chronometer is live. `Shift+M` handler runs ahead of the existing `KeyM` waypoint-AP toggle (which now requires `!e.shiftKey` to avoid double-firing) and is gated against intro / replay / photo so the reset doesn't fire from a menu or during a playback; allowed under pause / on-the-ground / crash so the pilot can zero the chronometer whenever they want. Reset fires a soft 880 Hz sine pip + `TRIP TIMER RESET · 00:00:00 · starts on next takeoff roll` status toast and replays the squawk pill's `sqFlash` border-pulse animation. `resetPlane()` wipes `tripSec` + `tripRunning` + the DOM text + the `.run` class so every fresh sortie starts at `00:00:00`. `onLanding()` + `onCrash()` now prefer `state.tripSec` over the airborne-only `flightStartMs` delta when writing the logbook row's `durSec` (Hobbs catches takeoff + landing rollout, the air-only counter misses both), with a graceful fallback when the trip was reset mid-flight. Added to `PHOTO_HIDE_IDS` so saved PNGs stay clean. Help strip grew the new `Shift+M reset trip timer` line.

- [x] Altimeter setting knob (Kollsman) — `Shift+,` decreases and `Shift+.` increases the altimeter setting in 0.01 inHg steps (clamped 28.00–31.00, defaults to 29.92 standard). Bind requires Shift so unmodified `,` keeps opening the settings panel; the settings-panel listener now gates on `!e.shiftKey` so the same press doesn't pop both. State holds `state.baroInHg` (persists across `resetPlane()` like a real altimeter that keeps its setting) plus a `state.baroSetMsgFired` one-shot session latch. Each nudge emits a `BARO XX.XX · altimeter setting` status toast for instant feedback. HUD `#alt` and the cockpit altimeter dial now display `altMDisp = altM + (state.baroInHg - _atisAltimeterInHg) * 1000 * 0.3048` — 1 inHg ≈ 1000 ft ≈ 304.8 m — so dialing 30.10 on a 29.92 field reads ~180 ft (≈55 m) high. Internal physics, AGL gates, terrain warnings, AP altitude capture, glideslope math, etc. all still use raw `altM` / `plane.position.y`, so the bias is strictly a pilot-facing readout. Cockpit altimeter renders a small `BARO 29.92` label on the bezel just below the digital meter readout (mimics the small Kollsman barometric window real 3-pointer altimeters carry). After an ATIS broadcast, `triggerAtis()` fires a one-shot `pushATC('SET BARO XX.XX', { tag:'TWR' })` reminder when `|state.baroInHg - _atisAltimeterInHg| > 0.05`, latched per session so a deliberately-wrong setting only gets pinged once. Help strip grew `Shift+,/Shift+. baro`.

- [x] Runway closed NOTAM — at scene boot, a one-roll session NOTAM (~30% probability) flags one ground airport (KHIL or KLAK — never the carrier `CV01` and never the active mission origin `MAIN_AP`/KSEA) as `RWY CLOSED`. Stored as a top-level `const notam = { apId, rwyDesig, markers: [] }` plus an `isAirportClosed(ap)` helper; no `state.*` additions, no localStorage. Visual: two crossed `PlaneGeometry(3,12)` strips rotated ±π/4 at each runway threshold, inset 30 m, parented to `ap._skidGrp` so they fade with the existing skid-mark plumbing. `MeshBasicMaterial` 0xffd24a, opacity 0.95, `polygonOffsetFactor:-2`, `depthWrite:false` so the yellow X's float just above the runway centerline paint without z-fighting. `triggerAtis()` appends `' NOTAM: RWY <desig> closed, expect grass arrival.'` to the broadcast string only when `notam.apId === ap.id`, so a pilot tuning ATIS at any other field hears the normal report. Good-landing branch switches `const score` → `let score`, deducts 100 (clamped ≥0), and sets `notamViolation = true` when the touchdown airport id matches `notam.apId`. After the existing `showStatus('GREASED IT'/'GOOD LANDING'…, 'good')` and the routine TWR welcome line, fires amber `showStatus('RUNWAY CLOSED', 'hard')` (auto-fades ~4.5s, satisfies the 3-second amber flash requirement) and `pushATC('Closed runway, RWY <desig>! Aborting.', { tag:'PILOT', now:true })`. The good-landing branch only fires once per touchdown so no extra latch is needed. Land card `reason:` becomes `'−100 NOTAM VIOLATION · landed on closed runway'` when violation is true, otherwise the existing empty default. Mini-map: `drawMinimap` prefixes the closed airport id with `'\u{1F6AB} '` (🚫) and draws a manual 1.4 px strikethrough line (`strokeStyle='#ff7676'`) across the measured text width. Quick-jump (J) overlay: the closed row stays clickable but renders with a new `.ap.notam` CSS class (opacity 0.55, amber tint), the meta column shows `🚫 CLSD`, and the name suffix grows ` · NOTAM`. `.ap.here` styling still wins over `.notam` so the row you're currently parked at always reads as the green active marker. Per design, the NOTAM does not block landing — even a hop-mission visit to the closed field still scores, just minus 100 — so free-flight pilots get a tiny dose of real-world chaos without losing the ability to actually touch down.

- [x] Pilot voice callouts — silent ATC text strip now sprouts a clipped-radio voice. New `speakVoiceLine(text)` in the existing SFX file (alongside `sfxReady`) builds a per-word formant-ish chain: every syllable spawns a sawtooth at F1, a sine at F2, and a higher sine at F3 picked by a tiny vowel-hint lookup so different vowels read distinctly, all routed through a shared `highpass(280) → bandpass(1400, Q=0.85) → lowpass(2600) → waveshaper` bus so the whole utterance sounds like one AM radio source instead of synthesizer test tones. Fricatives (`s/f/t/x/z/k/c`) get a 3–4 kHz bandpassed noise burst layered on the leading or trailing syllable, and the transmission opens + closes with a short squared squelch click so PTT release pops are unmistakable. Gated behind a new `VOICE` settings row (OFF / RADIO / FULL, default RADIO, persisted to localStorage). RADIO speaks only the priority calls — `ATIS` + `RA` tags always speak, plus a phrase scan for clearance/gear/wind shear/terrain/fuel/bird strike/mayday/engine fire/oil pressure/overheat/airspeed unreliable/brakes/carb ice — so the cockpit reads as a real radio instead of constant chatter. FULL speaks every line `_renderATC()` shows. Hooked from `_renderATC` (the same place the strip flashes) so audio syncs to text, wrapped in a try/catch so a synth glitch can never break the HUD. Photo mode hard-mutes via `state.photoMode` + the existing `settings.volume` gain so saved PNGs stay clean. A one-shot `_voiceNextStartT` latch queues utterances back-to-back instead of letting two voice lines stomp each other. Picking a non-OFF mode in the settings dropdown plays a `Radio check, voice radio/full.` self-test so the player hears what they just enabled.

- [x] Winds aloft layer — extended the existing surface-wind model with a separate upper-level wind that ramps in linearly between 600 m and 1800 m AGL. New `state.windAloft` / `state.windAloftTarget` vectors are re-rolled per session (and on every `resetPlane()` for `R` / quick-jump / new-mission) so each sortie sees a fresh upper-air pattern — surface wind keeps drifting + shearing as before for sock/cloud/particle consumers, the AI plane, and the bailout chute. Aloft direction is biased to within ±70° of the prevailing surface direction (so the layer reads as wind veering with altitude, not a totally unrelated random vector) at 10–22 m/s ≈ 19–43 kt so the shift is unmistakable when the plane climbs into the layer. New `effectiveWind(out, agl)` helper writes the blended vector into a scratch `state.windEff` Vector3 (allocation-free per step) and is the only thing the plane's `airVel` reads, so the cockpit actually feels the layer transition. HUD `WIND` row grows a thin second `↑WND <FROM>@<KT>kt` light-blue row (with an `AGL HI` label) that auto-shows only when the blend factor passes 5% — pattern-altitude pilots don't get a second readout cluttering the panel. Weather radar mini-panel grows a small light-blue arrow + `AGL HI` label in the top-right corner that mirrors the surface wind badge on the mini-map. Pairs naturally with the existing crab indicator + crosswind landing logic — descending through the blend layer now visibly wobbles the approach instead of staying glass-smooth all the way down. Per-frame cost is one extra `terrainHeight()` lookup and two `lerp`s inside the existing physics step.

- [x] Engine instrument startup self-test — every cockpit gauge needle now does a quick full-scale sweep (0 → max → live value) while the cold-start ritual is cranking, layered with a six-click mechanical tick train so the panel reads as a real avionics power-up instead of just "throttle unlocks". New `state.selfTestT` clock ticks from 0 → `SELF_TEST_S` (1.2 s) the frame `beginIgnition()` fires on a ground cold-start (skipped for in-air deadstick restarts so the pilot stays focused on flying), and is gated by the existing `settings.coldStart` + `photoMode` / `replayActive` / `paused` checks the preflight checklist toast already uses. `drawCockpitPanel()` reads `selfTestT` and overrides the values it passes to each draw call: ASI sweeps 0 → 400 km/h, ALT 0 → 10 000 m (both altimeter needles cycle), RPM 0 → 2 800 (via a new optional `rpmOverride` arg so the existing `state.coldStart` branch in `drawCockpitRpm` doesn't cap the needle at the 300-RPM cranking value), CHT 40 → 260 °C (firewall), OIL 0 → 100 PSI, MAG compass card 0 → 360°, BRAKE 30 → 400 °C, FUEL FLOW 0 → 18 GPH (also routed through a new optional `gphOverride` arg to bypass the cold-start GPH cap). Attitude indicator gets a synthesized pitch wobble via a temporary `THREE.Quaternion` so the horizon line visibly moves, then slerps back to the real plane attitude during the settle phase. First half ramps 0 → max, second half eases max → live so the needles visibly drop back to operating values before the engine catches at `COLD_START_SPOOL_S` (1.6 s). Underlying `state.engineTempC` / `state.oilPsi` / `state.brakeTempC` are never mutated — physics and HUD warnings see the real values throughout. Click train uses six soft triangle pings (1100 Hz climbing 35 Hz/step, ~50 ms spacing, ~35 ms decay) routed through the existing `sfxReady()` WebAudio path so no extra audio nodes leak. Cleared in `resetPlane()` and self-cleared in `step()` when the sweep completes. The cockpit panel now genuinely "wakes up" before every fresh sortie.

- [x] Cloud shadows on the ground — every cloud puff now lays a soft dark ellipse on the terrain directly below it. New `buildCloudShadowTexture()` bakes one shared 128 px radial-fade CanvasTexture so memory + draw cost stay flat regardless of cloud count; each puff gets its own `CircleGeometry(sx * 0.42, 20)` flat decal in a sibling `cloudShadowGroup` (sprite children inherit billboard space, so a peer group is cleaner). The existing cloud-drift loop in `step()` now snaps each shadow's x/z to its puff (wraps come along for free) and re-samples `terrainHeight(x, z) + 0.05` so the decal lays on hillsides without z-fighting (backed up by polygonOffset -1/-2). Opacity is recomputed per frame via a single shared `_shadowK = sunUp * rainK * fogK` multiplier (one `sunDirFor()` call hoisted out of the loop): shadows fade to nothing at night (sun below horizon → no contribution), drop to 25% in rain so overcast doesn't paint crisp blobs, and drop to 55% / 20% in light / heavy fog so the flattened lighting matches. Per-puff opacity multiplies in the captured `baseOpacity` (high clouds 0.45-0.70, low 0.70-0.95) so thicker clouds cast darker shadows; capped at 0.55 so even high-noon thick low clouds don't read as pure black. `shadow.visible` toggles off under 0.005 opacity so the renderer can cull cleanly at night and in heavy fog.

- [x] Touchdown zone lights at runway thresholds — paired rows of white inset bar markings (12 pairs per threshold, 30 m spacing, 3 m bar length, ~1.5 m off centerline) painted on top of the runway via `PlaneGeometry` strips in `makeAirport`'s new `paintTdzBars` IIFE. Each bar gets a small emissive glow head (SphereGeometry 0.35 m) that dusk-gates on the same `nightT` ramp as the runway edge lights (registered into a new `runwayTdzLights` array, updated inside `applyTimeOfDay()`'s existing edge-light loop), so the pattern punches through at night the same way the PAPI bars do. Pure white — unlike the yellow edge lights they never tint or flare on touchdown, matching real-world ICAO TDZ markings. Reads to a pilot on final as the standard ICAO touchdown aim point and pairs naturally with the painted runway numbers + PAPI bars so the whole runway finally reads like a real airfield at night instead of just two parallel light rails. Carrier deck is exempt by design (it's a synthetic runway built outside `makeAirport`).

- [x] Bingo fuel auto-suggest divert — when our straight-line range at the current burn drops under 1.2× the distance to the nearest ground airport (carrier intentionally skipped since a default "go to CV-01" on a water-shy bingo is worse than the existing low-fuel ATC nudges), `step()` fires a one-shot PILOT `"Bingo fuel, diverting <id>."` ATC call and force-sets `state.waypoint` onto that field so the existing magenta pin / bearing-and-distance / `M` heading-hold plumbing literally points the pilot at where to land. Gph proxy mirrors `drawCockpitFf()` and the mini-map fuel-range ring (2 + 14·(0.7t + 0.3t²) along the same `gph / 5760` per-sec drain at the ring's representative 210 km/h cruise) so the cockpit gauge, the dashed ring, and this auto-divert all agree on what "burn rate" means right now. Gated to free flight + engine live + fuel left + no existing waypoint (so a pilot already routing somewhere on purpose is never overridden) + non-crashed/replay/photo/intro/paused. `state.bingoLatch` is a one-shot per sortie; `resetPlane()` wipes it, and inside `step()` the latch self-clears once the cushion grows back past 1.6× (refuel pit stop, throttle pulled, divert completed) so a future leg can re-arm cleanly. HUD reads `BINGO FUEL · DIVERT → <id> · X.X km` so the pilot knows immediately where the magenta pin came from.

- [x] Visible engine exhaust stacks + flame puffs on full power — small dark cylinder stubs added to each engine cowl as visible exhaust pipes (two angled stubs flanking the nose cowl for single-engine; one stub per nacelle for twin), wired through `applyEngineConfig()` so the layout matches the prop config. Each stack carries a child additive-orange flame Mesh (small sphere stretched along the stack axis) that stays dark at idle, blooms in past ~70% throttle, and pulses with a fast `sin()` shimmer above 85% so full-throttle climbs visibly belch fire out the stacks. `finishIgnition()` arms a one-shot `state.exhaustCatchT = 0.45` flare so the cold-start catch and in-air deadstick restart both puff bright orange the frame the engine actually fires. The flame opacity write self-suppresses in photo / pause / replay / intro so saved PNGs never freeze mid-flicker, and the stack stubs are parented to `plane` so the existing cockpit-cam airframe-hide loop drops them out of cockpit view automatically (no view obstruction). Pure scenery + reward visual — no flight model side-effects, no new HUD, no audio.

- [x] Mid-air contact warning (TCAS-lite) — a new `tickTcas()` runs alongside `tickGearHorn()` in the per-frame step and reads off the existing AI plane + player kinematics, so no new draw calls. CAUTION trips when the AI plane is inside ~600 m AND converging (closure rate computed from `state.vel` minus `(AI_SPEED * heading)` projected onto the player→AI vector); pill paints amber `⚠ TRAFFIC` with a bearing arrow that rotates around the player nose (matching the EMR + RWY arrow convention) and a soft 720 Hz sine ping repeats every 700 ms. WARNING escalates either inside ~250 m (hard radius) OR inside ~450 m when closure > 8 m/s and vertical separation < 50 m — pill flips to red `warn-red`, the ping swaps to a sharper 780→1020 Hz square chirp at 320 ms, and a one-shot `pushATC('Traffic, traffic — resolve.', { tag:'TWR', now:true })` fires via `tcasAtcLatch`. Encounter latch clears once separation grows past 1 km so a future close pass re-arms cleanly without re-firing during a sustained formation loiter; soft hysteresis on the caution exit (1.1× caution radius OR closure flips to opening) prevents the pill from flickering on the boundary. Gated against intro / replay / photo / pause / crash / on-ground / AI culled past 12 km via the same checklist the formation tracker uses, hidden in photo mode via the existing `#hud` PHOTO_HIDE_IDS entry, and `resetPlane()` wipes the phase + ATC latch so each sortie starts clean. Turns the AI plane from a formation prize into a genuine traffic threat you actually have to watch.

- [x] Engine prime / mixture cycle as part of cold-start — extends the cold-start ritual with a true two-stage start. First tap of `I` arms a 0.5 s primer (`primeT` ticks in `step()`, plays `playEnginePrime()` — two filtered noise + sub-sine "chug-chug" pumps spaced 0.25 s apart), then the starter spool auto-rolls into the existing `playIgnitionCrank()` / checklist toast / instrument self-test path so the player only has to tap once for the full ritual. Tapping `I` a second time mid-prime force-starts the engine: it still catches, but `state.primed` stays false and `finishIgnition()` arms `state.roughIdleT = 3.0 s`. While rough, the cockpit RPM gauge layers ±180 RPM random judder on top of the base needle (fading linearly with the timer), the exhaust-flame opacity stutters with a randomized puff up to ~0.45 (`updateExhaustFlame()` gate added next to the catch-puff line), and `state.throttle * MAX_THRUST` gets a small `roughMul` bleed up to ~6 % so the engine feels like it isn't quite breathing right yet. Cold morning latch (`state.todHours < 7` captured at the moment of priming) adds `COLD_MORNING_EXTRA_S = 0.4 s` to the spool target so chilly starts genuinely take longer to catch. ATC pushes a `Priming engine.` line on the first tap and (on a rough start) a `Engine running rough — needed a proper prime.` line at catch. `state.primed` flips to true on first successful catch so the rest of the session (X-restart on the apron, etc.) doesn't demand another primer cycle — the fuel system is wet now. All four new state fields (`primed`, `primeT`, `roughIdleT`, `coldMorning`) reset in `resetPlane()` so every fresh sortie re-arms the ritual cleanly.

- [x] Magnetic compass card in the cockpit panel — NEXT entry was a duplicate of the already-shipped magnetic compass gauge (`cpMag` canvas in `#cockpitPanel`, `drawCockpitMag()` function with full floating-drum rendering, ANDS lag term off longitudinal accel × sin(hdg) for the classic whiskey-compass east/west turning error, smoothed via `state._magErr` so it doesn't jitter, lubber line + digital readout + MAG label, wired through the cockpit-panel tick alongside the other gauges); closed out without re-implementation so the same gauge doesn't ship twice. Same handling as the prior "Wind sock at airports" duplicate above.

- [x] Bird flock altitude warning toast — leading indicator for the existing bird-strike hazard. New `tickBirdProximity()` (runs alongside `tickGearHorn()` / `tickTcas()` in the per-frame step) sweeps the `birdFlocks` list, fires a one-shot `🐦 BIRDS, BIRDS — NN O'CLOCK` ATC line + amber status banner the first frame the plane drifts within ±200 m horizontal AND ±40 m vertical of a flock centre AND is closing on it at ≥ 5 m/s. Clock position is derived per-frame from the flock's relative position rotated into the plane's forward / right-wing basis (`fwdComp = dx*fwdX + dz*fwdZ`, `rightComp = dx*(-fwdZ) + dz*fwdX`, then `atan2(rightComp, fwdComp)` mapped to 1–12), so the bearing reads correctly through any bank/heading. Per-flock 4 s cooldown latch lives on the flock object itself (`lastWarnMs`, defaulted to `-Infinity` on construction and re-armed for every flock in `resetPlane()`), so a slow circle through bird country can't spam the strip but a fly-by past a *different* flock still fires. Gated against the same intro / photo / pause / replay / crashed / on-ground states the existing `birdStrikeAllowed()` checks so the warning never shouts in a frozen frame. Pure derived warning — no new visuals, no new physics surface area — turns the existing bird-strike hazard from a surprise wet thud into a chance to break away.

- [x] Cockpit dim/night-mode toggle (Shift+`;`) — Shift+`;` now flips the cockpit instrument panel into a red night-vision tint. A single new `applyNightPanelVisible()` helper toggles a `night` class on `#cockpitPanel`, and a small block of CSS does the per-gauge tint pass in one place (`#cockpitPanel.night canvas { filter: saturate(.15) sepia(1) hue-rotate(-45deg) brightness(.85) contrast(1.15) }`) so no gauge-draw code had to change — every ASI/AI/ALT/RPM/CHT/OIL/MAG/BRK/FF canvas inherits the same red wash with smooth `transition: filter .25s ease` on the toggle. Panel chrome picks up a `box-shadow: 0 0 22px rgba(220,40,30,.45)` red under-glow and a darkened `rgba(14,4,4,.78)` background so the bezel reads as "the panel is lit, not the cabin," and the gauge labels (`.glbl`) re-tint to a soft `#d56a6a` so they don't fight the wash. New `settings.nightPanel` boolean persists to localStorage on every toggle (default OFF), seeded into the live state on the initial `applyNightPanelVisible()` call right after `applyFogVisible()` so the panel comes up in the saved mode on reload. The Shift+`;` handler runs ahead of the existing trim path (the only paired binding given up is fine *nose-down* trim — plain `;` still nudges coarse trim and Shift+`'` still nudges fine nose-up, which most pilots reach for more often in cruise), shows a `COCKPIT NIGHT MODE` / `COCKPIT DAY MODE` status banner, and latches `state.nightPanelSuggested = true` so the auto-suggest line stays quiet once the player acknowledges. Auto-suggest lives in a new `tickNightPanelSuggest()` (runs alongside `tickGearHorn()` / `tickBirdProximity()` in the per-frame step): edge-triggered on the 20:00 TOD crossing via a paired `state.nightPanelLastTod` previous-frame sample, gated against intro / photo / pause / replay / crashed / `settings.nightPanel` already-on / non-cockpit-cam so the prompt only fires when it's both useful and visible. On a clean crossing it shows `NIGHT PANEL · press Shift+; to dim` and pushes a PILOT-tagged `Recommend night panel.` ATC line. Both latches reset on `resetPlane()` (the `settings.nightPanel` bit stays where the pilot left it, mirroring landing light + pitot heat), and the help strip + commented trim doc were updated to reflect the new binding. Pairs naturally with the landing-light auto-suggest at night.

- [x] Runway holding-point line + hold-short callout — Painted ICAO holding-position markings (two parallel solid + two parallel dashed yellow lines, ~12 m across the +x taxi strip with 0.55 m gap between the solid and dashed pairs) at each runway threshold at every ground airport, sitting 15 m back from the threshold along the taxi direction so the solid pair faces the apron (hold-side) and the dashed pair faces the runway (cross-when-cleared side, matching real ICAO convention). New `makeHoldShortLines(a)` group sits at world y = `a.elev + 0.046` with shared `0xffd166` paint (matches the chevron yellow), `transparent + depthWrite:false + polygonOffset` so the paint never z-fights the asphalt or the existing chevrons it sits between. Carrier deck is skipped automatically because CARRIER isn't in `AIRPORTS`. New `tickHoldShort()` runs alongside `maybeFireTakeoffClearance()` in the per-frame step: nearest-airport snap → local-frame transform (`lx = dx*cos - dz*sin`, `lz = dx*sin + dz*cos`, matches the marshaller / chevron math exactly so KHIL @ 0.35 rad and KLAK @ 1.55 rad hdg both work without special-case math) → ±5 m window around the painted hold-line z (`±(a.len/2 - 15)`) AND lx in `[a.w/2 + 2, a.w/2 + 20]` (player on the +x taxi strip, not on the runway). On a trigger frame: if brakes (regular OR parking) are set, fires `"<id>, line up and wait runway <DD>."` (player has committed to stopping at the hold point); else if ground speed > 5 km/h, fires `"<id>, hold short of runway <DD>."` (player is rolling onto the line uncommitted); slow + no brakes silently waits for either commitment. Designator comes from `runwayDesignator(near.hdg)` for the +z threshold and `runwayDesignator(near.hdg + π)` for the -z threshold so the call matches the painted runway numbers and the existing `<DD>-<DD>` taxiway-sign cluster sitting right next to the new paint. One-shot per departure via `state.holdShortFired` + `state.holdShortArmed`; re-arms on full-stop (gs < 0.8 m/s, mirrors the takeoff-clearance latch) so back-taxi-and-go and touch-and-go full-stop patterns each get a fresh call, and on `resetPlane()` so every fresh sortie re-arms cleanly. Gated against intro / photo / pause / replay / crashed via the standard `live` checklist. Closes the taxi-out beat between the marshaller wave and the takeoff-clearance call so the ground phase now has "marshaller in sight → hold short → cleared for takeoff" rhythm.

- [x] Replay scrub bar — the post-landing replay now ships with a horizontal scrub bar (`#replayScrub`) at the bottom of the screen: thin dark-blue track with a yellow fill + draggable knob, a `▶ PLAY / ❚❚ PAUSED` label, a live `current / total seconds` readout, and an inline hint strip showing the new shortcuts. Pointer events on `#rsTrack` cover click-to-seek and drag-to-scrub for mouse + touch + stylus in one path: `pointerdown` latches `state.replayScrubbing = true` + calls `setPointerCapture` so subsequent `pointermove` events keep firing on the captured element even when the cursor slides off the 14 px track, and `pointerup` / `pointercancel` clear the latch. `updateReplay(dt)` now freezes playback while either `state.replayPaused` (Space toggle) or `state.replayScrubbing` (drag) is true — the pose still re-applies from the current `state.replayT` so the scrubbed/paused frame renders correctly. A new replay-only keydown listener handles `Space` (toggle pause), `←` / `→` (nudge ±1 s clamped to `replayTotalSec()`); both call `updateReplayScrubUi()` for instant knob feedback. The existing skip plumbing is untouched: `R` and click-on-banner still `stopReplay()` immediately, and `startReplay()` / `stopReplay()` reset `replayPaused` + `replayScrubbing` so a fresh replay always starts playing from t = 0. Bar is photo-mode-aware via `PHOTO_HIDE_IDS` so saved PNGs stay clean, and tagged `touch-action:none` + `user-select:none` so phone drags don't trigger pull-to-refresh or text selection.

- [x] Crosswind takeoff coach line — `maybeFireTakeoffClearance()` now piggybacks a one-shot PILOT ATC `"Crosswind from the <left|right>, into-wind aileron."` line right after the existing TWR clearance call when the live crosswind component across the runway exceeds `XW_TAKEOFF_MIN_MS` (4 m/s, same threshold the visual `xwhint` arrow uses). Wind projection mirrors `drawXwTakeoffHint()` — plane nose vector horizontal-projected, pilot's right computed as `(-fz, fx)`, then `state.wind` dotted in so positive cross = wind blowing from pilot's left → call reads `"from the left"` → pilot holds aileron left, matching real-world technique. Latched on the existing `state.takeoffClearFired` so a calm-wind departure stays silent and a touch-and-go full-stop re-arms both the clearance and the coach piggyback together. Pure ATC injection — no new HUD, no new physics — but the takeoff side of crosswind technique now has a teaching moment that mirrors the crab indicator on landing.

- [x] Cockpit yoke / stick visual — in cockpit cam only, a small grey control column (cylindrical shaft + flat grip + two stubby side horns so the silhouette reads as a yoke and not a gear-shifter) is parented under the plane at plane-local (0, 0.18, 0.05) so it sits just ahead of the camera eye (which is at plane-local (0, 0.55, 0.4)) and below the look-at line (which targets (0, 0.4, -20)). The column pitches forward/back with the existing smoothed `elevatorAnim` and rolls left/right with `aileronAnim` — both already lerp at `SURFACE_RATE = 7/sec` and already include AP overrides + trim, so stick slams ease into the visual without any new smoothing code. Max visual deflection capped at YOKE_PITCH_MAX_RAD = 0.32 rad (~18°) and YOKE_ROLL_MAX_RAD = 0.55 rad (~31°) so the grip never punches through the instrument panel. The airframe-hide loop in `updateCamModeUi` now skips `yokeGroup` so the per-frame tick owns visibility instead of fighting it, and the tick gates on `state.cam === 1 && !state.photoMode && !state.paused && !state.replayActive && !state.introOpen` so saved PNGs, pause overlays, replays, and the intro screen stay clean. Pure visual feedback — no flight model change — but every keystroke / mobile-stick wiggle finally has a physical echo in the cockpit view, and the cockpit panel stops feeling like a static photo of an instrument cluster.

- [x] Engine start checklist hint toast — first frame on the ground after mission start, if the player taps `I` to crank with the parking brake OFF, fires a one-shot amber `⚠ SET PARKING BRAKE FIRST` status banner + soft PILOT ATC `"Parking brake before start."` line and silently swallows the crank (no starter SFX, no ignition). Real pilots set the brake before turning the key so the plane doesn't roll into the chocks; this finally enforces the habit without rewriting the cold-start ritual. Latched per session via `state.parkingBrakeHintFired` so it only nags once per cold-start, re-arms on `resetPlane()` and after a full-stop on the ground (piggybacks the existing `gs < 0.8` re-arm inside `maybeFireTakeoffClearance()`, mirroring the takeoff-clearance / hold-short latch pattern) so each fresh sortie can teach the lesson again. Pairs with the existing pre-flight checklist overlay (`Shift+K`) so the PARKING BRAKE row finally has teeth.

- [x] Time-of-day quick-set keypad (`Shift+T` in the air) — new `#timekeypad` modal that mirrors the existing `#sqkeypad` / `#jumpsel` chrome: dimmed full-screen backdrop, centered card, live `NOW HH:MM` readout, five preset rows (DAWN 05:30, MORNING 09:00, NOON 12:00, DUSK 18:30, NIGHT 22:00). Tapping a row — or typing `1`–`5` — snaps `state.todHours` to that value, also writes `state.todLastHours` and `state.nightPanelLastTod` so the per-frame sunrise/sunset toast detector and the night-panel auto-suggest don’t fire a stale crossing on the next frame, calls `applyTimeOfDay()` so the sky/sun/fog repaint immediately, pops a green `TIME OF DAY · DAWN · 05:30` status banner, refreshes the live readout, and auto-closes after 450 ms. Background click + `Esc` + `Shift+T` all close the modal cleanly. The Shift+T binding splits on `state.onGround`: ground presses still fire the existing warning-chime self-test (`startWarnTest()`), airborne presses route through `window.openTimeKeypad()` — the airborne-only split keeps the cold-start ritual and sunrise/sunset toasts from getting double-triggered on a fresh mission. New `state.timekeypadOpen` joins `state.sqkeypadOpen` / `state.jumpOpen` in the shared modal-gate checklist (Esc consumer, J quick-jump `canOpen()`, K squawk `canOpen()`, `startWarnTest()` guard) so opening one modal can’t stack on top of another. The row matching the current world hour (within ~15 min) auto-tags `.cur` so the player sees where the clock sits before they snap. `'timekeypad'` joins `PHOTO_HIDE_IDS` so saved PNGs stay clean. Help strip updated to `Shift+T test chimes (ground) / time keypad (air)`.

- [x] Engine cowl flap toggle (`Shift+F`) — toggleable cowl flaps that bias the engine cooling curve so the player finally has a real systems lever to manage cylinder temps instead of just throttling back. `Shift+F` flips `state.cowlFlapsOpen` (persisted to `settings.cowlFlaps`, default closed, seeded into state on init so the lever survives reloads); ground and air both honored, gated through `state.introOpen` so the main menu doesn't latch a stray key, and clean-split from the plain-`F` flap cycle so wing flaps still work. Open subtracts `COWL_FLAP_CHT_DROP = 25°C` from the existing `updateEngineTemp()` target right after the altitude bias — so a mid-throttle cruise that normally sits ~180°C settles ~155°C with the flaps spilling air, enough to keep a sustained climb out of the redline if the pilot remembers the lever. Drag side adds `COWL_FLAP_DRAG_BUMP = 0.04` to the `cd` term right next to the existing `dCDcanopy` / `dCDgear` / `dCDflap` chain, so cruise speed visibly drops on the SPD readout when the flaps are open. Cyan `❄ COWL OPEN` pill on the WIND row (next to CARB ICE, hidden in photo mode via the standard `state.photoMode` gate) reads in at a glance. Settings panel grows a new `COWL FLAPS` row right after WIPERS with a CLOSED/OPEN button that routes through `toggleCowlFlaps()` so the settings click and the Shift+F path share the same status-banner + latch + save plumbing (button label, `.primary` highlight, and live readout all flip in place via `syncInputs()`). Auto-suggest path lives inside `updateEngineTemp()` alongside the existing overheat warn-latch: first frame CHT crosses `COWL_FLAP_SUGGEST_C = 200°C` (well before the 230°C redline so the player gets a heads-up), fires a one-shot PILOT ATC `"Open cowl flaps, cooling marginal."` call + amber `COWL FLAPS · open to cool` status banner; `cowlAtcLatch` re-arms once CHT cools back under `COWL_FLAP_SUGGEST_CLEAR = 170°C` so a future hot event can re-prompt without spamming, and the latch starts pre-set when the flaps are already open (the lever is already doing its job, no need to nag). Closing in flight pops a green `COWL FLAPS CLOSED` toast via the same `showStatus()` path. `resetPlane()` preserves `cowlFlapsOpen` (it's a pilot setting like carb/pitot heat) but re-arms `cowlAtcLatch` from the current open state so each fresh sortie's first overheat can still teach the lesson. Help strip updated to include `Shift+F cowl flaps` right after the existing `Shift+C carb heat` entry. Pure systems surface — no new mesh, no new audio — but the existing engine-temp gauge + overheat damage path finally has a counterpart lever instead of being a one-way "throttle back or die" prompt.


- [x] Wingtip vortex condensation streamers — short trailing wisp of white sprite puffs spawning from each wingtip whenever AOA exceeds ~6° AND a humidity proxy is high (rain on OR within ~10 in-sim minutes of dawn/dusk). Two new per-side sprite pools (32 puffs each) mirror the existing reverser-dust pool pattern so memory cost stays bounded. Emit rate ramps linearly with AOA from 8 Hz per wingtip at 6° up to 28 Hz at 14°+, so a gentle climb in dewy morning air leaves a thin streamer and a hard pull-up on short final over a wet runway throws a thick visible wisp. Each puff lives 0.8 s, starts at 0.18 m radius and grows to ~0.73 m while alpha eases from 0.65 to 0 so the trail reads as a fading wisp, not a smoke trail. New `updateWingtipCondensation(dt, aoaDeg)` runs right after `updateVortexTrails(state.vortex)` (so `aoa` is in scope) and self-gates on `state.photoMode / paused / replayActive / introOpen / crashed / onGround` — sprites aren't DOM elements, so the standard PHOTO_HIDE_IDS list doesn't reach them, but the emission gate keeps photo mode pristine the same way airshow smoke + reverser dust pools do. `wtcHumidProxy()` is the gate (settings.rain OR `|todHours − 6| < 1/6` OR `|todHours − 18| < 1/6`, mirroring the sunrise/sunset toast hours), and the emit-rate accumulator resets the frame humidity flips off so a brief flap-down doesn't spit a burst when the rain comes back on. `resetPlane()` calls `clearWingtipCondensation()` next to `clearReverseDust()` so a fresh sortie starts wisp-free. Pure visual flavor that finally rewards a hard pull-up over a wet runway with the airshow-photo money shot, without any new physics surface.

- [x] Stopwatch / chronograph in the cockpit panel — tenth small canvas added to the existing gauge cluster (`cpChr`, 220×220, labeled `STOPWATCH`) showing a digital `MM:SS.t` running clock plus a small `LAP ...` window underneath. Interaction lives entirely on the canvas so no keybinding is taken (the spec's `;` / `'` are already trim and Shift+`'` is fine nose-up trim, so click was the safe pick): single click on the gauge toggles running, right-click records the current elapsed time into the LAP window (running-only so a paused chrono doesn't multi-fire the same split), double-click zeros both the elapsed and lap clocks. New `state.chrono = { running, elapsedMs, lapMs }` lives next to `state.selfTestT`; ticks only inside `drawCockpitPanel()` (skipped during the avionics self-test sweep so the chrono doesn't "ride along" the gauge power-up) so the clock only advances when the cockpit panel is actually on screen — that also gives photo / pause / replay / intro free for-free, since `drawCockpitPanel()` early-returns when the panel is hidden. Gauge face matches the rest of the cluster: shared `_cpBezel()` + `_cpNeedle()` helpers, sweep-second needle hand on the outer ring with major 0/15/30/45 ticks + minor 5s ticks, `CHRONO` label up top, MM:SS.t readout in green when running / dim grey when stopped, amber `LAP MM:SS.t` underneath (or `--:--.-` placeholder until the first lap is recorded). `_formatChrono()` clamps display to 59:59.9 and the tick accumulator clamps state to 3599999 ms so a forgotten clock can't blow the layout or accumulate forever. `resetPlane()` wipes it next to the trim reset so each fresh sortie starts on a clean clock. Pure scorekeeping for free-flight pattern work, no new physics, no new audio.

- [x] Approach-slope deviation tape on the HUD — new slim vertical strip (`#aslope` canvas, 38×160 px) pinned to the right edge of the HUD just left of the existing angular meatball (right:218px, top:308px, matching the VSI / glide column rhythm). Reads altitude error in METRES vs. the 3° glideslope to the nearest runway, scaled to ±200 m: center line = on slope, top = +200 m high, bottom = −200 m low. Companion to the meatball — the meatball is angle-focused, this tape reads in actual metres so pilots can call "on slope, 30 m high" without scanning the altimeter. New `drawSlopeDevTape(hdgDeg)` (slotted between `drawILSNeedles(hdg)` and `drawCrabIndicator(hdg)` in the per-frame draw chain) mirrors the meatball's runway-pick logic over `runwayInfos` (lateral / along-runway / aim-point math, score = `horiz + |lx|*1.5 + |dh|*40`) so the two indicators agree on the active runway; activation rules match the spec — within 5 km of a runway, gear down OR landing-light on, aligned within ±60° of runway heading. Target altitude on the 3° slope at the live range is `aim.elev + horiz * tan(3°)`; `errM = plane.y - targetAlt` (+ve = high, −ve = low). Marker color tracks the meatball thresholds (green <25 m, amber <75 m, red low / white high) so cross-reading reads consistently, drawn as a stubby horizontal bug with a centered notch so it's at-a-glance distinct from the meatball's diamond pip. Includes ±50 m / ±100 m tick rungs, HI/LO tip labels, signed metric readout at the bottom (`+18m` / `−5m`), and off-scale white-up / red-down chevrons that mirror the heading-bug / meatball pattern. Hidden by default — the function flips `display:block` only when active — and added to `PHOTO_HIDE_IDS` so saved PNGs stay clean. Pure derived HUD readout off existing plumbing; no new physics, no new audio. Pairs with the meatball + ILS cross-needles so the right edge of the HUD finally tells the full glideslope story at a glance.

- [x] Throttle quadrant in the cockpit yoke view — companion to the existing cockpit yoke. New `throttleGroup` parented under the plane at plane-local (0.12, 0.16, 0.05) so it sits just to the right of the yoke and within the cockpit FOV: small grey BoxGeometry quadrant base (0.06×0.10×0.12 m) with a thin black lever (cylindrical shaft + spherical knob) hinged at the top of the base via a nested `throttleLeverPivot`. Lever swings on the same `SURFACE_RATE = 7/sec` lerp the yoke uses (new `throttleLeverAnim` 0..1 eased toward `state.throttle` each frame) from `+THROTTLE_FWD_MAX_RAD = 0.35 rad` at idle (lever tipped toward the firewall) to `-THROTTLE_BACK_MAX_RAD = -0.85 rad` at full power (lever pulled back toward the pilot's seat), matching real throttle-quadrant convention where pulling back gives more power. Asymmetric travel (more aft than forward) mirrors real GA quadrants where the lever has more room to swing toward the pilot than toward the panel. Visibility gate is identical to the yoke (`state.cam === 1 && !state.photoMode && !state.paused && !state.replayActive && !state.introOpen`) so saved PNGs, pause overlays, replays, and the intro screen stay clean. The airframe-hide loop in `updateCamModeUi` now skips `throttleGroup` (alongside the existing `yokeGroup` exception) so the per-frame tick owns visibility instead of fighting it. Pure visual feedback — no flight model change — but every `W` / `S` keypress and mobile throttle slider drag finally has a physical echo in the cockpit so the throttle setting doesn't feel disembodied anymore. Pairs with the yoke visual so the cockpit view now reads as a real pilot station: stick under one hand, throttle under the other.

- [x] Generator / alternator failure event — random ~1-in-25 sorties the alternator quits mid-flight, turning every electrically-driven readout into a fading battery-only countdown until the pilot can land. The roll is fired exactly once per sortie about 30 s after takeoff (gated on `flightStartMs` so a deadstick or quick touch-and-go can't constantly re-roll, and so a save-scummer who taps `R` mid-flight doesn't get to reroll the result — the choice was made the moment they committed to the flight). On a hit, the trip itself is scheduled a random 30–180 s later (`altFailScheduledMs`) so the pilot can't predict when the lights go out. When the trip fires, `state.altFailed = true`, the HUD spawns a new amber `⚡ BATT-ONLY MM:SS` pill in the warn row (live counts down from 3:00), the cockpit instrument panel (`#cockpitPanel`) gets a `.alt-fail` class that dims the gauge canvases ~25 % via a CSS `filter: brightness(.72) contrast(.92) saturate(.85)` and drops the gauge labels to 72 % opacity, the entire HUD overlay gets the same `.alt-fail` class for matching 75 % opacity, the panel fires a one-shot CSS keyframe flicker (`@keyframes altflick` cycling brightness from .18 → .95 → .10 → .55 → .18 over 0.18 s in 2 steps so it reads as honest electrical noise rather than a smooth fade), `showStatus('⚡ ALTERNATOR FAILURE', 'running on battery — land soon', 'bad')` posts the player banner, and `pushATC('Alternator out, running on battery, declaring emergency.', { tag:'PILOT' })` puts the call on the ATC strip. Once on battery, `state.altOnBatteryT` ticks up real seconds; every 4.5–11 s the panel re-runs the flicker keyframe so the failure feels alive instead of being a step function, and the pill's `MM:SS` readout updates every frame so the player can see the deadline closing. At `ALT_BATTERY_FULL_SECS = 180` s (3 min) the panel gets `.alt-dark` (canvas filter drops to brightness .18 contrast .55 saturate .25, labels to 32 % opacity), the HUD swaps its `.alt-fail` for `.alt-dark` (opacity .30), the pill flips from amber (`warn-amber`) to red (`warn-red`), and a second banner + ATC line fires (`'⚡ BATTERY DEPLETED'`, `'instruments dark — fly visually'`). Composes cleanly with night-mode: a dedicated `#cockpitPanel.night.alt-fail canvas` selector pre-multiplies the night sepia/hue-rotate with the dim brightness so a night-flight alt-fail still reads as red panel + dimmed rather than fighting itself. Critically, the engine itself keeps running the whole time — the prop is mechanical, the alt is electrical — so the player can still navigate by eye toward the nearest runway; the failure is a systems-management emergency, not an engine failure dressed up in new clothes. New `updateAlternator(dt)` runs in the per-frame tick hub right after `tickNightPanelSuggest()` and self-gates on the same emergency-context guards as fire/gear-horn/birds (`state.crashed / replayActive / photoMode / introOpen / paused`); landing auto-clears all the visual classes the moment the wheels touch (`if (state.onGround) { strip classes; reset counters; return }`) so a successful save-the-day landing visibly restores the panel even though the `altFailed` bit stays true for the sortie. `resetPlane()` wipes every new state field next to the engine-fire reset block (`altFailed / altRollDoneMs / altFailScheduledMs / altOnBatteryT / altFlickerNextMs`) and strips the panel/HUD classes inline so a hot reset gets a fresh roll on the next flight with zero one-frame flash. Five new constants control the feel (`ALT_FAIL_CHANCE = 0.04`, `ALT_ROLL_DELAY_S = 30`, `ALT_FAIL_DELAY_MIN_S = 30`, `ALT_FAIL_DELAY_MAX_S = 180`, `ALT_BATTERY_FULL_SECS = 180`, plus `ALT_FLICKER_MIN_MS / MAX_MS = 4500 / 11000`) so a future tune is a one-line edit. Pure additive — no existing emergency (fire / fuel-out / bird strike / gear horn) changes behavior, but the player now has a fourth flavor of bad day to manage on top of the existing systems, with the rarity tuned (1-in-25) so it's a memorable event rather than a constant nuisance.

- [x] Mini-map zoom levels (Shift+wheel / pinch) — mini-map now ships with three zoom levels: CLOSE (±3 km radius / 6 km wide), DEFAULT (±10 km / 22 km wide, the legacy scale), and WIDE (±25 km / 50 km wide). Wheel-on-mini-map cycles between them (wheel-up = zoom in, wheel-down = zoom out; Shift modifier optional so a casual scroll over the badge also cycles), and a two-finger pinch on touch does the same job (spread = zoom in, pinch = zoom out, re-baselines after each step so a slow sustained pinch can step through every level). New `state.minimapZoom` (0/1/2) drives a `currentMinimapRange()` helper that drawMinimap, pxToWorld (waypoint clicks), and the dblclick airport-snap all read through, so every consumer agrees on the live scale automatically — no per-feature zoom plumbing. Range rings rescale per zoom (1 km step on CLOSE, 5 km step on DEFAULT, 10 km step on WIDE) so the CLOSE view still shows multiple rings and the WIDE view doesn't clutter the face. Small top-right corner badge in the mini-map prints `ZOOM xN` (relative to DEFAULT, so DEFAULT prints a clean `x1`) + the active level label (CLOSE/DEFAULT/WIDE), painted in amber when off-default so the player always knows which scale they're on. Each cycle pops a `MAP ZOOM · <LABEL>` toast with the new ±km radius. Wheel handler `preventDefault`s so a scroll landing on the small mini-map face doesn't accidentally scroll the page. Pinch handler shares scope with the existing waypoint long-press logic and cancels the pending long-press the frame a second finger lands so a pinch can't accidentally drop a pin. Sticky session preference (no localStorage — zoom is a quick visual toggle, not a long-term setting). Pure HUD plumbing — no new physics, no new audio.

- [x] Route planner — chain of waypoints (Shift+click on mini-map). Shift+click on the mini-map now appends a new pin to the route instead of replacing the active one (plain click still toggles single-pin / clears the whole route, matching legacy muscle memory). The HUD WPT row shows `WPT 3/5` while a route is active, the mini-map draws a dashed magenta line through every leg with small numbered diamonds for chained pins, and reaching the active pin auto-promotes the next leg into `state.waypoint` so AP heading-hold (M) keeps tracking without re-arming. Chain capped at 12 pins with a 50 m de-dup so a runaway shift-click streak can't spaghetti the map. `Shift+M` now wipes the whole route when one is set and falls through to its legacy trip-timer reset when no pins exist. Bingo divert, double-click airport jump, and resetPlane/startMission all clear the chain too so stale legs never fight an emergency target or a fresh sortie. Help strip updated to document Shift+click chain + Shift+M clear.

- [x] VOR receiver in the cockpit panel — eleventh small canvas (`cpVor`, labeled `VOR / CDI`) added to the cockpit gauge cluster next to the chronograph. Picks the nearest airport in `AIRPORTS` as the VOR station each frame (carrier deck CV-01 is intentionally excluded — a steaming flat-top isn't a fixed beacon), computes the radial FROM the station to the plane via `atan2(dx, -dz)` (matches the rest of the sim's clockwise-from-north convention), and deflects a vertical CDI bar left/right proportional to how far that radial is off the selected OBS course. Full-scale at ±10° with five 2/4/6/8/10° dots either side of center, exactly matching real VOR receiver scaling. TO/FROM flag falls out of the standard real-world rule (|dev| ≤ 90° → FROM, else → TO), and when TO is showing, the CDI deflection sign auto-flips so the pilot still "flies the needle" correctly toward the chosen course (the cone-of-confusion flag-flip right over the station is preserved — that behavior is also accurate). Selected OBS course drives a rotating compass card underneath a fixed yellow lubber line + triangle pointer at top (mirrors the magnetic compass drum pattern from the neighbouring gauge), so the chosen course number is always readable under the lubber and the cardinal points (N=red, E/S/W=amber) read upright as the card spins via counter-rotation on the labels. OBS is set entirely by clicking on the canvas — click anywhere around the dial sets the OBS to the bearing of the click point (`atan2(dx, -dy)` from gauge center, snapped to whole degrees), and right-click snaps OBS onto the current FROM radial (one-tap "center the needle" so the pilot can quickly establish where they are relative to the station). All interaction lives on the canvas itself so no new keybinding is taken, matching the chronograph pattern next door. New `state.vor = { obs, station }` survives `resetPlane()` (pilot setting, mirroring landing light / pitot heat / carb heat / cowl flaps) so a series of practice flights doesn't keep clobbering the OBS the pilot dialled in. Lower-left of the gauge shows green `TO` / dim grey `FROM`, lower-right shows the live `OBS NNN°` digital readout (turns green when the CDI is on-course inside ±2°), and the station identifier (KSEA / KHIL / KLAK) prints up top under the lubber so the player always knows which beacon they're tracking. Pure derived readout off the existing airport list — no new physics, no new audio — but pairs with the existing ILS cross-needles + meatball + slope-deviation tape for full-on instrument-approach practice from any direction, not just the runway-aligned final.

- [x] Engine air-restart procedure — the X-key in-air branch now models a real deadstick restart instead of just a quick toggle. New `airstartRpm = Math.min(900, spdKmh * 4)` matches the cockpit RPM gauge's windmilling formula exactly, so the same needle the pilot is staring at also gates the restart attempt: `airstartRpm > 300` (≈ 75 km/h) AND `state.throttle > 0.25` arms the existing `state.airRestart` latch and routes through the unchanged `beginIgnition()` / `playIgnitionCrank()` / `finishIgnition()` ritual (still ~1.6s spool, still the same crank SFX). Below the airspeed threshold fires a `AIRSPEED TOO LOW FOR RESTART` red status banner + `dive for speed — windmill the prop` subtitle + PILOT-tagged `"Airspeed too low for restart, build airspeed."` ATC line so the pilot is told the actual recovery technique (trade altitude for airspeed). Above-threshold-but-throttle-at-idle fires a separate `THROTTLE TOO LOW FOR RESTART` banner + `"Restart failed, advance throttle past idle."` ATC line so the pilot also gets coached on the second half of the procedure (push throttle so cylinders have a mixture to catch on). Reverses the legacy `throttle < 0.30 + spdKmh > 80` rule (which incorrectly gated restart on a pulled-back throttle); the new rule is the real-world checklist order (windmill the prop first, then mixture/throttle forward, then crank). Pure rule swap inside the existing X-key handler — no new state, no new SFX, no new HUD surface — but every fuel-out / engine-fire / X-killed sortie now has a genuine moment where the pilot has to nose-down to keep the prop alive instead of just flipping a switch.

- [x] Wind shear escape guidance — `tickWindShear()` now piggybacks two new beats the same frame a shear burst flips active on short final: a PILOT-tagged ATC `"Wind shear escape — max power, pitch for climb."` line right after the existing TWR `"Wind shear on final, RWY <DD>."` alert (so the player hears the alert and the recovery technique back-to-back on the strip), and a new green centered `↑ NOSE UP · ↑ MAX POWER` banner (`#shearEscape`) with a `WIND SHEAR ESCAPE` subline that pops via the existing `missionPop` keyframe and auto-hides after 4s. Banner CSS mirrors `#missionDone` (same position rhythm at top:30%, same border/shadow weight, same animation) but recolored to `#7ee787` green so it reads as `do this NOW` instead of `you finished something`. `showShearEscape()` helper lives right next to `showMissionDone()` and re-arms its timer on every call so a touch-and-go re-entry into shear (rare; per-burst latch already keeps it one-shot per approach) still pops. Hidden in photo mode via the standard `PHOTO_HIDE_IDS` array entry. Pure ATC + banner — no physics change — but teaches the real-world wind-shear-escape procedure (max thrust, pitch for max climb performance, do NOT chase the airspeed indicator) the same way the takeoff-clearance coach teaches into-wind aileron.

- [x] Approach plate kneeboard (`Shift+N`) — right-side ivory-paper clipboard overlay (`#kneeboard`, top:50% / right:12px / width:256px, monospaced Jeppesen-style numbers on a `#f1e8d3` background with a `#c2b48a` border) that flips in when Shift+N is pressed and out on Shift+N or Esc. Header shows the nearest in-range field's ID + name + chosen runway designator; briefing rows print `RWY / HDG / THR ELEV / LEN`, then a `3° GLIDESLOPE` table with `ALT MSL` and `AGL` columns at 1 / 2 / 3 / 5 km derived from `Math.tan(3°) × dist` plus airport elevation (matches the in-cockpit glideslope meatball math exactly), then a live `BRIEFING` block with current `WIND` (using the same projection math `_atisRunwayInUse` uses so the panel and ATIS always agree, formatted as `DDD° @ NN kt` rounded to nearest 10° or `CALM` under 3 kt), `ALTIM` (from `_atisAltimeterInHg`), `TIME` (formatted off `state.todHours` as `HH:MMZ`), and `RANGE` (km from field). Wind-favoured threshold auto-picks via dot-product of the wind vector against each runway end (matches ATIS / windsock logic so a runway change shows up live as wind rotates). Top-down plan-view canvas underneath draws the runway as a `#1c2733` rectangle pointing UP (rotated so chosen landing direction = canvas up), a red threshold dot, the standard left-hand VFR pattern dashed in `#1c5a8a` (1500 m offset downwind on the LEFT, 1800 m base ahead of threshold, 1200 m upwind off the departure end, crosswind link) with tiny chevron arrows mid-leg so the pattern reads as a flow, a 1 km scale bar bottom-left, a north arrow upper-left labeled `N`, a wind arrow upper-right colored `#c0392b` showing direction wind is blowing TOWARD in plate frame, and a green/red triangle marking the player's live position rotated by the plane's yaw so the dot's pointed end matches where the nose is aiming (turns red and clamps to the canvas edge when the plane is outside the ±5 km view). Out-of-range path (>12 km from any field) hides the plan canvas + GS table + briefing rows and shows a single `No airport in range · fly within 12 km of a field` note so the panel never lies about stale numbers. New `state.kneeboardOpen` flag + `setKneeboardOpen()` / `toggleKneeboard()` / `updateKneeboard()` triad models on the existing preflight (`Shift+K`) pattern — the panel is pointer-events:off so it never eats clicks, NOT listed in `isAnyOtherModalOpen()` (it's a HUD overlay, not a modal: doesn't pause the sim, doesn't block input), and auto-closes inside `updateKneeboard()` on intro / replay / photo / pause / crash / jump-panel / squawk-keypad / time-keypad so it never lingers across a state the sim wants to own. Toggle gate mirrors `togglePreflight()` plus the J / squawk / time keypad checks so a stray Shift+N during a keypad press can't sprout the kneeboard underneath the modal. Esc handling lives in the existing global Esc handler (closes kneeboard BEFORE opening the pause menu so the player's instinct — "open with Shift+N, dismiss with Esc" — just works without sprouting the pause card on top). Added to `PHOTO_HIDE_IDS` so saved screenshots stay clean. Per-frame `updateKneeboard(false)` runs right after `updatePreflight(false)` near line 20165; both early-return on `!open` so closed-panel frames pay basically nothing for the DOM writes / canvas redraw. Help strip line gains a `Shift+N approach plate` reference next to the existing `Shift+K pre-flight` keybinding. Pure derived readout off the existing airport / wind / ATIS plumbing — no new physics, no new audio — but finally gives the player a real PDF-style briefing card to glance at on the way into a field instead of holding the whole approach in their head.

- [x] Engine mixture lever (`-` lean / `=` rich) — adjustable mixture (`state.mixture`, 0 = full cutoff, 1 = full rich, default 1.0, persisted to `settings.lastMixture`) that biases engine thrust + CHT + fuel burn off altitude. `mixtureTargetForAlt()` decays linearly from 1.0 at sea level to ~0.55 at 4500 m so each altitude has a sweet-spot mixture; `mixtureOffBand()` returns a single normalized 0..1 "how far off the band" number that the thrust path multiplies in as `mixMul` (steals up to MIX_THRUST_BLEED_MAX=15%), CHT path adds in as a target bump (peaks at MIX_CHT_BUMP_MAX=22°C above 1500m when running over-rich), and fuel-burn path applies as `mixFuelMul` (lean bonus saves up to MIX_FUEL_LEAN_BONUS=25%; full-rich high-alt cost is MIX_FUEL_RICH_PENALTY=8%). Pulled below MIX_LEAN_ROUGH_THR=0.30 the thrust also drops linearly so the engine starves — down low (where full-rich is correct), a too-lean lever feels like the engine is about to quit. New `adjustMixture(delta)` is the single entry point for the keypress handlers — mirrors `toggleCarbHeat()` for status-toast / settings-save pattern — and `Minus` (lean by 5%) / `Equal` (rich by 5%) bindings live next to the carb-heat key handler. **Bound to `-`/`=` instead of the roadmap's original `,`/`.` because plain `,` already opens the settings panel and Shift+`,`/Shift+`.` is the Kollsman altimeter knob; `-`/`=` is the universal sim convention (FSX / X-Plane / MSFS all use these for mixture) so the choice still matches what a real-pilot player will reach for**. HUD adds a `MIX X%` pill on the WIND row (green when on-band, amber when |mixture - target| >= MIX_HUD_BAD_THRESH=0.22) so the player gets a constant readout + learn-by-color cue. One-shot PILOT ATC `"Lean mixture for altitude."` call fires the first frame the plane climbs past MIX_ATC_ALT_HI=2500 m with mixture still >= MIX_ATC_FULL_RICH=0.85, latched on `state.mixtureAtcLatch` and re-armed once the plane descends below MIX_ATC_ALT_RE_ARM=2000 m. Cockpit panel grows a small red knob silhouette on its own `mixturePivot` to the right of the throttle lever (real-world convention is the mixture knob is red) that lerps with the live setting on the same axis as the throttle, hidden via the same throttleGroup gating so it stays in cockpit cam only. `resetPlane()` clears the ATC latch (matches carb-heat reset pattern) but preserves the mixture lever itself — it's a pilot setting, not a per-flight state. GPH gauge re-derives the same fuel-mix multiplier so the cockpit fuel-flow needle matches the actual tank drain, which means a properly leaned cruise at altitude visibly drops the GPH needle and stretches the fuel-range ring on the mini-map. Help strip + preventDefault key list (added `Minus`/`Equal`) updated. Pairs with the existing fuel-flow gauge + range ring + CHT gauge — the high-altitude cruise is now a real GA fuel-economy puzzle instead of a one-knob throttle exercise.

- [x] Crash debris field — every crash (terrain impact, gear-up slam, mid-air airframe failure) now spawns a fixed pool of 12 dark-grey `BoxGeometry` shards (per-shard random 0.4–1.2 m on each axis with tiny tint variance so the pile reads as broken panels) at the impact point with randomized outward velocities scattering 5–15 m, individual tumble spin, gravity-fall + ground bounce at 35% restitution + 4.5/s horizontal friction + 2.0/s angular damping until each shard "settles" at `DEBRIS_SETTLE_SEC = 1.5 s` (vel/ang vel zeroed). Shards inherit 40% of the plane's pre-impact horizontal momentum + 20% vertical so a high-speed wreck scatters downrange instead of dumping a pile straight on the impact point. A thin oily black smoke column (separate 60-sprite pool that reuses the airshow puff texture, dark `0x1a1a1a` tint with per-puff jitter) emits at `CRASH_SMOKE_EMIT_PER_SEC = 5` from `crashDebris.origin` for `CRASH_SMOKE_TOTAL_SEC = 12 s` then ages out cleanly over `CRASH_SMOKE_LIFE = 12 s` total, drifting with `state.wind` and rising 0.6 m/s as hot smoke. New `spawnCrashDebris(world, baseVel)` is called from `onCrash()` (typeof-guarded so the function-hoisting order doesn't bite the early call at line 1446), `updateCrashDebris(dt)` is called from `loop()` after `updatePlaneShadow` (NOT inside `step()`) so shards still settle and smoke still drifts during the post-crash 30s `startReplay()` playback — otherwise everything would freeze in mid-air the instant the replay started. `clearCrashDebris()` wipes shards + smoke and is called from `resetPlane()` (next to `clearEngineFireFx()`) so a hot R-restart on the wreck pad starts on a clean slate. Photo mode hides the entire field (`hidden = !!state.photoMode`) so saved PNGs stay clean, matching the engine-fire FX suppression pattern. Pure additive — no new mesh per frame, no new audio, no per-frame allocations. Pairs with the existing replay so the orbit cam over the wreck finally has something to look at, and pairs with the crash card so the diagnostic + visual punctuation sit side by side.

- [x] Pilot voice callouts toggle in settings — NEXT entry was a duplicate of the already-shipped broader voice system (`SETTINGS_DEFAULT.voice` with OFF / RADIO / FULL modes, `VOICE_MODES` table, `_voiceShouldSpeak()` filter, `speakVoiceLine()` formant synth wired from `_renderATC()`). RADIO mode (the default) already routes every `tag: 'RA'` push through the radio-voice synth, which is exactly how the radalt callouts at line 16341 (`500` / `300` / `Minimums` / `100` / `50` / `30` / `20` / `10`) fire — so the co-pilot already reads each number out loud over the existing chime + ticker. The synth is a clipped-radio formant emitter (chained sines at F1≈380 / F2≈1700 Hz with vowel-dependent biases + a noise envelope per syllable through a 300–2400 Hz bandpass + light waveshaper), each token pitched off `_voiceWordVowelHint()` so `"five hundred"` reads lower than `"ten"` thanks to the different vowel formants. Hidden in photo mode via the existing `if (state && state.photoMode) return false;` check inside `_voiceShouldSpeak()`. Settings panel already has the VOICE row at line 875 with the three-state cycle persisted to localStorage. Closed out without re-implementation so the same feature doesn't ship twice (same pattern as the earlier `Wind sock at airports` duplicate above).

- [x] Visible windsock animation at airports — the existing single-cone wind sock now splits into TWO chained 2.25 m cone segments (mouth → mid → tail) hanging off `tiltGrp`, each with its own small Z-axis bob driven by `Math.sin(performance.now()*0.001 * 2π·WINDSOCK_RIPPLE_HZ + phase)`. Base segment leads, tip segment trails with a π/2 phase lag so the wave visibly travels mouth → tail like real cloth ripple instead of one rigid swing. Per-frame yaw flicker (±~3°) layered on top of the steady wind-heading yaw at ~60% of the bob frequency so the jitter doesn't sync with the segment wave. Both amplitudes ramp in from a 3 m/s threshold so dead-calm stays glassy; a 12 m/s wind visibly flaps the cloth while a 2 m/s sock just hangs and sways. Per-sock random `phase` so adjacent airports never ripple in lockstep. Mouth hoop + white accent band + linear taper preserved at rest so the silhouette is unchanged from the prior frozen pose — only motion changes. Pure additive scenery polish: no flight model touch, no new audio, no per-frame allocations (single `performance.now()` read + 6 `Math.sin` calls reused across all 3 airports).

- [x] Touch-and-go best streak toast — the T&G touchdown branch now snapshots `pilotStats.bestTgStreak` into `_tgPriorBest` BEFORE bumping it, so the existing post-bump compare still records the new lifetime peak and the new toast can fire on the same touchdown. When the live `state.tgStreak` is ≥ 2 AND the prior best was already ≥ 1 AND the live streak ties or beats `_tgPriorBest`, `showToast('🔥', 'NEW BEST STREAK ×N', …)` fires with the right copy for tie vs. beat (`Tied your lifetime best — one more for the record.` vs. `New personal best — keep them coming.`). One-shot per record level falls out naturally because the streak only ever increments by 1 per landing, so a streak that walks 5→6→7→8 against an old best of 5 fires exactly once at each new level. Gated to free-flight + T&G modes the way the existing `#scTgRow` HUD pill is, and hidden in photo / pause / replay / intro / crash via the same checks the formation / sunrise / pattern toasts already use so saved frames stay clean. Reuses the existing `showToast` host + 4.4 s auto-fade so no new DOM or CSS.

- [x] Time-of-day reverse step (`Shift+T`) — mirrors plain `T`'s +1-hour forward step with a −1-hour rewind that fires the same `🌅 Sunrise` / `🌇 Sunset` toasts in reverse (rewinding past hour 18 announces sunset, rewinding past hour 6 announces sunrise). Lives BEFORE the existing `Shift+T` routing (warn-test on ground / time keypad in air) so both legacy paths still fire on the same press — the rewind is a free pre-action that costs nothing if the player is reaching for the keypad or chime test. Silently ignored while the warning chime self-test is actively running (so an in-progress chime cycle doesn't fight a stray rewind) or while the time-of-day quick-set keypad is open (the keypad's snap presets already cover absolute-time jumps). Re-applies the live time-of-day curve through `applyTimeOfDay()` the same frame so the world tone shifts immediately instead of waiting a frame for the per-step lerp. Help strip updated to advertise `Shift+T = -1h` next to the existing `T = +1h` line.

- [x] Logbook export to clipboard — the LOGBOOK panel's actions row gained a `📋 COPY` button between CLEAR and CLOSE that serializes the full `pilotLog` array (date, mission, origin→dest route, duration, max altitude, score, verdict) into a fixed-width plain-text table and ships it to the clipboard. Async `navigator.clipboard.writeText()` is the primary path (covers the HTTPS GitHub Pages deploy); on rejection or in legacy / non-secure browsers it falls back to an off-screen `<textarea>` + `document.execCommand('copy')` so the button still works everywhere. Success fires a `LOGBOOK COPIED · <N> flights` toast through the existing `showStatus` host (singular for N=1), failure shows `LOGBOOK COPY FAILED · clipboard unavailable`, and an empty logbook short-circuits to `LOGBOOK EMPTY · no flights to copy yet` so the player isn't left wondering whether a no-op copy actually fired. Reuses `fmtLogDate`, `fmtFlightTime`, and `MISSION_LABELS` so the exported rows mirror exactly what the modal renders — paste into Discord and the route + verdict read the same as on-screen.

- [x] Engine misfire on low fuel — when fuel drops below 5% the engine starts intermittently coughing: while a pop window is live, `misfireMul` collapses available thrust to 30% for 80–200 ms per cough, the cockpit RPM gauge slumps ~900 RPM with a small fluttery jitter on top, and a new `playFuelMisfirePop()` one-shot fires a low sine "thup" layered with a short low-passed noise burst through the existing `sfxReady()` path. The scheduler arms the first frame fuel slides under 5% with a 3–6 s lead-in (so the cough doesn't step on the existing `Fuel critical` ATC call the same frame the latch fires), then ramps the gap from 3–6 s at 5% fuel down to 1–2 s as fuel approaches 0% so the rate visibly accelerates the closer to dry. A new amber `⚠ FUEL STARVATION` pill flashes in the warnings column the whole time the scheduler is armed (not just during the cough window) so it doesn't strobe in step with each pop. Suppressed in photo / replay / pause / intro / crash and gated against `engineOut` / `coldStart` / `refueling` / `onGround` so a parked tank-out can't fire phantom pops; refuelling past 5% wipes the scheduler so the next leg out re-arms cleanly. Pure additive — the existing `state.fuel <= 0` branch still owns the final deadstick, so the sequence now climaxes in audible + felt engine failure instead of running silent until the prop windmills.

- [x] ATIS broadcast on tune (`Shift+A`) — Shift+A now toggles a small ivory-paper #atisStrip card on the left side that mirrors the kneeboard chrome (same #f1e8d3 paper tint, monospaced font, dashed-rule sections) and reads the nearest field's `INFO <LETTER>` (hour-cycled off `state.todHours` via the new `_atisInfoLetter()` helper so the broadcast and strip can't disagree), active runway (`_atisRunwayInUse`), wind, altimeter, ceiling (`OVC 008` on rain / `BKN 005` on heavy fog / `SCT 020` on light fog / `CLEAR`), visibility in km (30+ clear, 3 km light fog, 1 km heavy fog, knocked another 25% by rain), OAT (`_atisTempC()`), Zulu time, and slant range to the field. Opens at 25 km, auto-closes the second the plane drifts beyond range or on intro/replay/photo/pause/crash/keypad-modal, mirrors the kneeboard's Esc-to-dismiss handler, hidden in photo mode via PHOTO_HIDE_IDS, and the audio broadcast still self-rate-limits via the existing 4.5s `atisCooldownUntil` latch so a held Shift+A opens once and stays put without chime-spam. Pure derived readout off existing wind / weather / runway-favoring plumbing — no new physics, no new audio paths.

- [x] Hand-flown ILS course needle override (`Shift+V`) — the keypad-modal scaffolding was already in place (DOM, CSS, `state.ilskeypadOpen`, `state.ilsLockId`, `_ilsLockIdFor` / `_ilsResolveLock`, `drawILSNeedles` lock branch, `resetPlane()` clear) but never actually wired to a keybinding or an open path. New `setupIlsKeypadUi()` IIFE at the tail of the script enumerates every runway end out of `runwayInfos` on each open (so future airports light up here for free), builds a scrolling preset list with an AUTO row plus one row per end (`KSEA RWY 36 — Sea Field`, `CV01 RWY 09 — CV-01 Pacific`, etc., labelled by the same `_ilsLockIdFor()` helper the cross-pointer matches against), and tags the row that currently matches `state.ilsLockId` as `cur` so the active lock reads at a glance. Click a row or press the matching digit and `applyById()` sets `state.ilsLockId`, fires a one-shot `PILOT` ATC line ("ILS locked, KSEA36." / "ILS unlocked, auto."), pops a `showStatus('ILS LOCK', ...)` chip, then auto-closes after 450 ms. `0` selects AUTO (clears the lock); `1`–9 select the first nine rows; rows past 9 stay clickable via the scrolling list. `Shift+V` toggles open/closed and `Esc` / background-click dismiss; clears the held `KeyV` entry on both open and close so the cockpit quick-look bind doesn't keep the head yawed while the modal sits up. Gated through the same conservative `canOpen()` as the squawk + time keypads (no intro/replay/photo/pause/crash, no other modal on top), and the existing `PHOTO_HIDE_IDS` registration already hides `#ilskeypad` in photo mode so saved PNGs stay clean. Pairs naturally with the VOR receiver and pattern-entry guide for hand-flown instrument practice.

- [x] Density altitude readout in the cockpit panel — twelfth small canvas (`cpDa`) showing pressure altitude vs density altitude in feet. Each frame `updateDensityAltitude()` computes OAT off a `±15°C around 18°C` cosine swing on `state.todHours` (peaks at noon, bottoms just before sunrise) minus the standard ISA lapse rate (2°C per 1000ft), then `DA = PA + 120 × (OAT − ISA temp)`. Needle paints green below 2000 ft DA, amber 2000–4000 ft, red past 4000 ft so hot-day takeoffs read at a glance; digital `DA <n>` headline + smaller `PA <n>` reference both rendered below center. New `state.densityAltMul` ramps from 1.0 to `DENSITY_ALT_THRUST_FLOOR` (0.88) across DA 2000–8000 ft and folds into the live thrust formula (`thrustMag * densityMul`), so a hot-and-high takeoff visibly struggles to spool, climb, and roll out short. Self-test sweep includes a `SWEEP_DA = 10000` ramp so the gauge participates in the cold-start avionics power-up alongside the other ten cockpit gauges. Reset wipes the cached needle so a fresh sortie boots from zero. ~140 LOC, no new assets.

- [x] Cockpit panel night lighting — when the sun drops toward the horizon (same dusk gate as the runway edge lights: `nightT = clamp(1 - (sunDirFor(todHours).y - (-0.05)) / 0.3, 0, 1)`), the cockpit panel chrome + gauge bezels + glb labels pick up a warm amber post-light wash so the instruments read like real GA cockpit lighting at dusk/dawn/night instead of disappearing against the black sky. New `let _cockpitDuskT` (module-level scalar) lerps toward the live `nightT` each frame inside `drawCockpitPanel()` with a glassy ~1.5 s ease (`(dt||0.016) * 0.7`) so the fade is smooth either direction; intensity publishes to a `--duskAmt` CSS custom property on `#cockpitPanel`. Per-frame `cpEl.classList.toggle('dusk', _cockpitDuskT > 0.01)` switches a single class on the panel so the CSS owns all the visual work. New `.dusk` rules drive: (1) an amber halo `box-shadow` around each gauge canvas scaled by `calc(14px * var(--duskAmt))` for the instrument-post glow, (2) gauge bezel `border-color` lerped from the cool `#1e3548` daylight to a warm post-light tint via `rgb(calc(30 + 60 * var(--duskAmt)), calc(53 + 35 * var(--duskAmt)), calc(72 - 18 * var(--duskAmt)))`, (3) panel chrome amber rim glow + slightly warmer background, and (4) `.glbl` label color lerped from cool teal to warm amber. Crucially the `.dusk` rules touch only `box-shadow` / `border-color` / `color` / `background` — *no* `filter` — so the existing `.night` (Shift+; red panel), `.alt-fail`, `.alt-dark`, and `.alt-flicker` filter chains compose unchanged. Belt-and-suspenders: a `#cockpitPanel.dusk.night` override suppresses the amber chrome wash when red night-vision mode is on so the two lighting moods don't fight. Photo mode already hides the panel via `PHOTO_HIDE_IDS`, so saved PNGs stay clean for free. ~45 LOC of CSS + ~14 LOC of JS, no new assets.

- [x] Squelch / radio static between ATC lines — every `_renderATC()` line now triggers a tiny two-layer mic-key burst right before the voice synth speaks: a ~14 ms narrow bandpass-noise click at ~2.4 kHz to sell the PTT key-down, then an ~80 ms bandpass-noise tail at ~1.6 kHz that fades out as if the carrier is releasing. New `playRadioSquelch()` SFX reuses the shared `sfxReady()` WebAudio context (one `createBuffer` per layer, one `BiquadFilterNode` per layer, one `GainNode` per layer — no new hardware sinks), volume tracks `settings.volume` at 0.10× so it sits under engine + voice without stomping either. New `_atcSquelchTimes` ring buffer (capped at 4) + `_maybePlayATCSquelch()` enforces the spec’s rate limit: 5th firing inside 2 s gets eaten so a flap + gear + wind-shear cluster doesn’t machine-gun the speaker. Gated against `state.photoMode` and against `#atc` being hidden via `style.display === 'none'` (which is exactly how `setHudHidden()` + `PHOTO_HIDE_IDS` already hide the strip), so saved PNGs and any future explicit hide path stay silent. Hooked in `_renderATC()` between the flash retrigger and the voice-synth speak, double-wrapped in `try { ... } catch (e) { /* never let audio kill the HUD */ }` so any audio glitch can’t take down the visible HUD. Turns the ATC ticker from a silent text crawl into something that feels like a real radio every time a line lands. ~85 LOC.

- [x] Wind-shift tower call on landing roll — `updateLandCardRollout()` now fires a one-shot TWR advisory the first frame ground speed drops below 25 km/h on a runway if the current wind direction has shifted > 30° since the previous landing at that airport. Latch key is `state.lastLandWind[apId]` (initialised to `{}` next to `state.burgerAp`, intentionally NOT wiped on `resetPlane()` so the comparison is between sorties, not within one rollout). Per-rollout `d.windShiftFired` flag prevents re-checking on subsequent slow ticks. Call uses the same FROM-degrees convention as `_windCall()` and the HUD readout (`atan2(-wx,-wz)`, rounded to nearest 10°): `"<id> winds now <DDD> at <KT>, advise."`, or `"<id> winds now calm, advise."` below 3 kt. Gated to non-carrier (`AIRPORTS.indexOf(proj.ri.ap) !== -1` skips the synthetic CV-01 deck), non-crashed, on-runway via `landingProj().onRunway`. Stamp always runs (even when delta ≤ 30° or no prior baseline) so the NEXT rollout has a fresh reference. Wrapped in try/catch like the existing arrival call so a stray ATC glitch can never block the land card. ~40 LOC.

- [x] Co-pilot voice callouts in the pattern — three soft callouts fire on the standard left-hand pattern when `settings.copilot` is ON (default). New `playCopilotChirp()` SFX (G4→D4 two-tone muted-sine pulses ~330 ms total at 0.18× volume with a low-passed triangle partial @ 2× freq for the muffled cockpit-speaker timbre) layered with a new `tag: 'CP'` ATC strip push: `"Gear check, abeam the numbers."` on the downwind abeam moment, `"Base turn, flaps two."` on the base turn, and `"Final, on speed."` rolling out on final. Detector lives in `step(dt)` right after the radalt approach-callout block (line 17236 onwards), gated by free/tng mode, no crash/replay/photo/intro/pause/onGround, within 5 km of any non-carrier `AIRPORTS` field and between 30 m and 1500 m AGL. Position is computed in pattern-entry-guide airport-local coords (`al = sinH*dx - cosH*dz`, `la = -cosH*dx - sinH*dz`, landing direction = −al, left side = la < 0) so a callout only fires when the plane is actually flying the rectangle the visible guide drew. Abeam fires the frame `al` crosses `+halfLen` upward with `la` clearly on the downwind line (between -PG_LAT×0.45 and -PG_LAT×1.6) and `vAl > 6`; base fires once base heading dominates (`vLa < -6`, `|vAl| < |vLa|×1.5`) past `+HALF + 400`; final fires once `|la| < 250` with `vAl < -8` and `|vLa| < |vAl|×0.6`. Three independent latches in `state.coPilotArmed.{abeam,base,final}` keyed off `state.coPilotApAirport` so a different runway gets a fresh set; full set re-arms above 200 m AGL (same `cpAgl > 200` threshold as the gear-horn / goAroundArmed rearm) or on `resetPlane()` / setting flipped ON. Out-of-range (>5 km) clears the airport latch so the next descent starts fresh. New COPILOT settings panel row with the same toggle-button pattern as COLD START / TWIN; persisted via `SETTINGS_KEY`. Pairs naturally with the existing radalt callouts (which fire as `tag: 'RA'`) since the new tag rides the same `pushATC` queue but with distinct text and a distinctly softer chirp.

- [x] Distance-to-go ribbon during Airport Hop — when an Airport Hop mission is active, a thin pill pins at the bottom-center of the HUD showing `LEG <n>/<total> · NEXT <id> · <km.k> km · ETA <m:ss>` (ETA off current ground speed; reads `—:—` under a 5 m/s floor so taxi/hover don't print garbage). km figure color-codes green inside 5 km, amber 5–20 km, blue past 20 km. Auto-hides on the final touchdown (hopDone latches), in photo/replay/pause/intro/crash, when mode isn't `hop`, and whenever the touchdown/crash/bail card is up so the bottom-center stays clean. Sits at `bottom:180px` so it clears `#papi` (18), `#xwhint` (62), and the cockpit gauge panel (54+~120) in cockpit cam. Listed in `PHOTO_HIDE_IDS` so saved PNGs stay clean. Mission already tracked the next waypoint internally — this just surfaces it instead of making players squint at the mini-map between airports.

- [x] DME readout next to the VOR gauge — small dark digital window painted in the upper-left quadrant of the existing `cpVor` canvas with three lines: `DME N.N nm` (slant range from the current VOR station to the plane, computed via `Math.hypot(dx, dz)` × 0.000539957 m→nm), `GS NNN kt` (groundspeed from `state.vel` horizontal magnitude × 1.94384), and a dim `ETA M:SS` row (range / groundspeed, clamped to 99:59, blanks to `--:--` under 30 kt so taxi doesn't make the readout jitter). Anchors to the same station the existing VOR receiver picks (nearest non-carrier airport), so a right-click on the gauge that snaps OBS to the live FROM radial also implicitly re-anchors the DME — no separate frequency picker keeps the UI quiet. DME range number color-codes green when range is falling frame-over-frame (closing on the station), amber when rising (opening), with a 1m/frame deadband so a perfect 90° crossing doesn't flicker; neutral white on the first frame after a station change. New `state.vor.lastRangeM` sentinel (-1) handles the no-station path. Window placed in the upper-left quadrant clear of the centered station ID label up top, the horizontal CDI dot scale across y=cy, the vertical OBS course line at x=cx, and the lower-left TO/FROM flag so the gauge stays legible at every OBS setting. Pairs with the existing VOR + ILS cross-needles so the cockpit panel now answers "where am I / how far / how long?" in one row of glass.

- [x] Active-runway change suggestion toast — when the wind-favoured threshold at the nearest in-range airport (within 12 km, matches the kneeboard's KB_RANGE_M) flips to the OPPOSITE end of the runway the player is currently lined up on (heading within ±35° of either threshold course, matches the meatball / crab alignment cone), a one-shot amber `🔄 RWY CHANGE · <id> favours RWY <DD>` toast fires alongside a PILOT-tagged ATC `"Wind shift — RWY <DD> in use."` call. New `_favoredRunwayFlip(ap)` helper extracts the dot-product projection ATIS / windsock / kneeboard already share so the four reads always agree on which threshold wins. `tickRwyChangeSuggestion()` runs after `tickWindShear()` on a 30s throttle that re-arms immediately if the wind FROM direction has swung > 30° since the last evaluation, gated cruise-only (skips crashed / replay / photo / intro / paused / onGround) with a 3 m/s minimum wind floor (calm days don't bother). Per-airport `state.rwyChange.latched` keyed by airport id keeps the same wind condition from spamming after the player declines to switch; the latch map clears on `resetPlane()` (mission restart), `teleportTo()` (J / Shift+J quick-jump), and the moment ground speed drops below 0.8 m/s on a full-stop rollout (the existing `updateLandCardRollout` hook). Off-runway / off-approach cruise gets nothing so the toast only surfaces when the choice actually matters — the existing wind sock + ATIS + crab indicator stack finally becomes an active coaching loop instead of a passive backdrop.

- [x] NDB beacon + ADF needle gauge — twelfth small canvas in the cockpit panel cluster (`cpAdf`, matched to the existing chrono / VOR sizing) showing a classic rotating-card ADF with a fixed lubber line at top, a tail-and-head needle (yellow head + arrowhead chevron, grey tail, white hub) that always aims at the bearing of the tuned beacon FROM the plane in the plane's reference frame so a "fly the head of the needle" gesture takes the player straight there. Three new NDBs live in a fresh `NDBS` array (`BPN` Bayport at (4500, 5500) ~7 km from KSEA, `RYE` Ryeford at (12500, -10500) ~6.4 km from KHIL, `WLK` Westlake at (-10500, 14500) ~7 km from KLAK) so any inbound or outbound from any ground field naturally passes one en route. Mini-map renders each beacon as a small magenta diamond + 3-letter ident, dim (alpha 0.55, dusky `#7a4a6a`) by default and bright (alpha 1.0, `#ff7be5`) when tuned; off-map beacons get a small rim diamond pointing toward them, matching the airport rim-tick treatment. ADF defaults to AUTO mode (`state.adf.station = -1`) which auto-picks the closest beacon each frame; left-click the canvas cycles AUTO → BPN → RYE → WLK → back to AUTO so the pilot can lock onto a specific NDB to practice tracking, and right-click snaps back to AUTO for a fast bail-out. Bottom of the gauge prints the live beacon id + distance (`820 m` / `1.4 km`) + mode tag (`AUTO` cyan vs `MAN` amber). `state.adf = { station: -1 }` survives `resetPlane()` (mirroring VOR / pitot heat) so the pilot's tuning persists across sorties. Pure derived readout off the NDB list — no new physics, no new audio (real ADFs are silent unless you tune the morse, skipped) — but pairs with the existing VOR + ILS cross-needles so the cockpit finally has the full classic IFR navigation stack and the world has navigation aids OUTSIDE of just the runway approaches.

- [x] Snow weather toggle in settings — new SNOW row in the settings panel (OFF / LIGHT / HEAVY) mutually exclusive with RAIN: toggling one auto-clears the other (in either direction) so the world never tries to render snow falling through rain streaks at the same time. When active, the existing `#rainOverlay` canvas swaps painters — `updateSnowOverlay()` draws slower, larger white flakes (LIGHT 140 / HEAVY 280) drifting near-vertically at ~30–45% of the rain streak speed with a gentle per-flake `sin(time + phase)` horizontal sway, and wind + airspeed nudge the field sideways through the same capped lateral tilt the rain painter uses. Runway, apron, taxiway, and ground-airport materials re-tint toward a brighter cold blue-white (`#dfe7f0`) via the same `applyRunwayWetness()` lerp the rain runway-condition path uses (snow pushes `wetK + 0.25` so the strip reads as accumulating, not just damp). Friction model takes a heavier hit than rain: snow brakes 0.30× dry (vs rain 0.55×) and free-rolling friction 0.50× dry (vs rain 0.70×), so a snowy rollout slides much farther and demands real rudder discipline. HUD shows a soft blue `❄ RWY SNOW` pill on the WIND row next to the existing `RWY WET` pill so pilots can read the surface state at a glance. Carrier deck stays exempt (deck heaters + arrestor wires) but still tints visually so the look and feel stay consistent across surfaces. Persists to localStorage as `settings.snow` with defensive load-time mutual exclusivity (snow wins over rain if both somehow survive a save). Hidden in photo mode via the existing `PHOTO_HIDE_IDS` entry for `#rainOverlay`. Pure renderer + friction knob — no new physics, no new audio — but finally turns the night/cold-TOD/pitot-icing window into a full winter scenario instead of just a colder daytime palette.

- [x] Engine monitor / EGT page in the cockpit panel — `Shift+G` (gear stayed on plain `G`, so the keymap is back to one-key-one-action) now toggles `state.engineMonitorOn`, swapping the round ENG TEMP + OIL gauges out of the cockpit instrument cluster for a wider `cpEng` rectangular tile that shows four vertical EGT bars (one per cylinder) plus a single CHT bar mirroring the existing CHT gauge. EGT values are derived purely off the live throttle + mixture: idle ~1050°F, ramps to ~1500°F at full throttle with a mild S-curve, and the existing `mixtureTargetForAlt` / `mixtureOffBand` helpers bias the readout the way real engines move — lean-of-peak climbs toward peak-EGT then drops off; rich-of-peak runs cooler. Per-cylinder ±2% jitter via two summed low-freq sines (~0.7–1.4 Hz each, phase-offset per cylinder) so each bar wobbles on its own rather than the row marching in lockstep, and the amplitude scales with throttle so idle is glassy and full power has visible motion. The hottest cylinder picks up a yellow `LEAN` flag so the pilot can chase peak-EGT mixture practice. Color thresholds (green/amber/red at 1550/1620°F) and a redline tick on the CHT bar mirror the round-CHT gauge palette so the panel stays color-coded the same way across both views. The overlay auto-hides while the engine is dead, out of fuel, on fire, or during the avionics self-test sweep so it never masks an actual emergency — the CSS `.eng-mon` class is toggled each frame inside `drawCockpitPanel()` so the swap reverts the moment the engine goes silent. `state.engineMonitorOn` is intentionally preserved across `resetPlane()` (same pattern as `landingLight` / `cowlFlapsOpen` / `carbHeatOn`) so a checklist-heavy pilot doesn't have to re-toggle each sortie. Pure derived readout off the existing engine model — no new physics, no new audio — but finally rewards the lean-of-peak / rich-of-peak mixture decision the carb-heat + cowl-flap + mixture-lever stack has been hinting at since shipping.

- [x] Engine-out best-glide HUD coach — when the engine is dead (mixture-killed, fuel-out, or starved out by an engine fire) and the plane is above ~50 m AGL, a cyan `BEST GLIDE 105 km/h` pill appears on the HUD with the live IAS color-coded green within ±5 km/h, amber within ±15, red outside that band so the pilot can pitch to the target instead of guessing. One-shot `"Establish best glide."` PILOT ATC call fires the first frame the engine quits airborne; the latch (`state.bestGlideAtcLatch`) clears on `resetPlane()` and the moment the engine is live again on the ground (refuel / restart on the apron) so a subsequent mid-flight failure re-arms the call. Hidden in pause / photo / replay / intro / crash / on-ground so it can't ghost during cleanup states. Turns the existing engine-out + engine-fire scenarios from "oh no" into a real skill check — pilots learn the real-world memory item without any new physics.

- [x] Nearest-airport diversion arrow — emergency-only HUD pill at the top of the main panel: yellow chevron + `→ <id> 4.2 km` lights up the instant fuel drops below 20%, the engine quits, or the engine catches fire. Chevron rotates around its own center to point at the nearest field's nearest runway threshold (any airport, picks closest by straight-line; carrier deck included since CV-01 is a legit emergency divert for a fuel-starved sortie), reusing the same per-runway threshold scan as the existing RWY pill so both readouts stay coherent. Auto-hides inside a 1 km airport zone so the mini-map + checklist take over there, hides on the ground / in cleanup states (intro / replay / photo / pause / crash) so it can't ghost, and rides inside `#hud` (already in `PHOTO_HIDE_IDS`) so saved PNGs stay clean. Pairs naturally with the existing low-fuel ATC call, the bingo-fuel auto-divert waypoint, and the BEST GLIDE coach — panicked pilots now get a single glance answer to "which way home?" without having to squint at the mini-map.

- [x] Touch-and-go streak counter — new cross-session `state.landStreak` counts every successful runway touchdown (any mode) without a crash in between, completely separate from the existing T&G "lift-off within 12s" streak. New `🔥 ×N` HUD pill appears under the existing T&G streak row in the TOUCH & GO score panel once the streak hits 2, color-tiered white → amber (≥5) → magenta (≥10) so the climb feels visible. Crashes call `onCrash()` which now zeroes `state.landStreak` and fires a one-shot `💔 STREAK BROKEN` toast (`Lost a run of N clean landings.`) when the prior streak was ≥2 — gated to non-photo / non-replay / non-pause / non-intro so saved frames stay clean. Lifetime peak persists to a new `pilotStats.bestLandStreak` (in `STATS_DEFAULT` so the existing RESET button + save/load loop pick it up for free) and surfaces on the PILOT STATS screen as a new `BEST LANDING STREAK` row right below the existing `BEST T&G STREAK`. Gamifies the existing landing pipeline for free flight without inventing a new mission, and gives Touch & Go mode a reason to keep going past the timer.

- [x] Cockpit yoke/stick visible in cockpit cam — NEXT entry was a duplicate of the already-shipped cockpit yoke (the `yokeGroup` + `yokePivot` rig built around line 4755: a 0.20 m grey cylindrical shaft topped with a flat grip disc and two side horns so the silhouette reads as a yoke rather than a gear-shifter, positioned just ahead of the cockpit eye and below the look-at line so it lives in the lower-center of the cockpit FOV). The per-frame tick around line 16725 drives `yokePivot.rotation.x = -elevatorAnim * YOKE_PITCH_MAX_RAD` and `yokePivot.rotation.z = -aileronAnim * YOKE_ROLL_MAX_RAD` off the smoothed surface state (which already absorbs AP overrides + trim via `SURFACE_RATE`), so the wheel tilts forward/back with elevator and twists left/right with aileron exactly as the NEXT entry described. Visibility is gated to `state.cam===1 && !state.photoMode && !state.paused && !state.replayActive && !state.introOpen` and the airframe-hide loop in `updateCamModeUi` skips `yokeGroup` via the exception list at line 22514 so chase/orbit/flyby cams stay clean. The matching cockpit throttle quadrant (`throttleGroup` + mixture knob, also DONE) lives in the same rig and shares the same gating. Closed out without re-implementation so the same control column doesn't ship twice (same pattern as the earlier `Wind sock at airports`, `Fuel-flow gauge in the cockpit panel`, `Magnetic compass card in the cockpit panel`, and `Pilot voice callouts toggle in settings` duplicates above).

- [x] Magnetic variation realism on the compass — fixed `MAG_VAR_DEG = 15` (°E) baked into a pair of `trueToMagDeg` / `magToTrueDeg` helpers (and a `magToTrueRad` shortcut), so the HUD compass strip, top-center HDG readout, whiskey card, ADF card, and heading bug all now read magnetic (≈15° lower than the underlying true heading). Physics + autopilot stay in true world-frame heading: `state.ap.hdgBug` is interpreted as magnetic everywhere it's stored / displayed, with `magToTrueRad` converting to the true `state.ap.hdgRad` the heading-hold loop actually flies. Updated touch points: AP engage snap (true → mag for the bug + toast), `[` / `]` nudge + heading-hold tick + compass-strip click (mag → true on the way down), waypoint follow (true bearing → mag bug), HUD `#hdg` readout, and the `drawCompass` / `drawCockpitPanel` call sites in `render`. Relative-needle math (ILS, glideslope, crab, ADF needle) is invariant under the offset — mag−mag and true−true produce the same delta — so those untouched. New `MAG VAR` row in the settings panel between AUDIO and LIVERY shows the live `+15°E` value pill (derived from `MAG_VAR_DEG` so the docs can't drift) and a hint explaining that runway designators / ATIS stay on the legacy true-heading math, so the two-digit RWY won't match the magnetic course exactly.

- [x] Stars in the night sky — celestial dome at radius 16000 (inside the 20000 sky shell) carrying ~400 dim white-blue point sprites uniformly distributed on the upper hemisphere plus a brighter cluster of 30 "named" stars biased toward the zenith. Single 32×32 radial-gradient CanvasTexture feeds both `THREE.Points` meshes (additive blending, depthWrite off, fog off, renderOrder -1 so the field paints right after the sky shell). New `tickStarDome(dt, nightT)` runs right after `applyTimeOfDay` and drives all of: visibility (hidden whenever `nightT <= 0.001` so day frames pay nothing), dim-layer global opacity (`0.85 * nightT` so the field eases in through twilight on the exact dusk gate the runway edge / TDZ lights use), bright-layer per-vertex opacity (sin² twinkle on a 1.4 Hz beat with per-star phase offsets stored in a Float32Array so the cluster never blinks in lockstep), and a whole-group counter-rotation around world Y at `-todHours * π/12` so one in-sim day = one full opposite revolution and a long dusk→dawn sit visibly sweeps stars across the sky. Bright layer uses a tiny ShaderMaterial (vertex color attribute + DynamicDrawUsage `aOpacity` attribute + `uGlobalAlpha` uniform) so per-star twinkle and global dusk fade stack in one draw. Pure scenery — zero new audio, one tex, two Points meshes, no per-frame allocations.

- [x] Runway end identifier lights (REIL strobes) — a pair of bright synchronized white strobes flanking each runway threshold at every ground airport (4 strobes per field) plus a single pair at the carrier deck's stern (approach end), 14 strobes total. Each strobe is a 0.28 m emissive sphere on a slim 1.6 m white post with a 6.5× additive `_reilGlareTex` sprite child that carries the visual from 6–8 km out. Pulse cadence is locked to `navStrobePhase` + `NAV_STROBE_*` so airport REIL double-flashes in perfect lockstep with the player's own wingtip strobes — every airport blinks with your wings. Dusk gate (`REIL_NIGHT_FALLOFF = 0.35`) matches the runway edge / TDZ / PAPI / beacon family: completely off at noon (no random white grass dots), faint base glow + bright strobe pops from dusk through dawn. `tickReilStrobes(dt)` runs right after `tickNavLights` in the per-frame loop so both consume the same advanced phase value. Hot path is one `sunDirFor()` call + a 14-iteration loop with no allocations.

- [x] Mountain wave / ridge lift updraft hazard — pool of 8 invisible 800m-wide vertical-air cylinders anchored to deterministic offsets off 8 of the 28 distant mountain peaks (same `hash2()` seed the ring mesh uses, so anchors never drift). While the plane is inside one above 1200m MSL, a smooth +/-5 m/s vertical velocity gets lerped into `state.vel.y` with linear horizontal + vertical falloff so the bump fades in / out at the rim instead of latching. Sign is decided per pocket by dotting the pocket's stored offset direction against the live wind direction: pockets on the windward face of a peak get an UPDRAFT, pockets on the lee face get SINK — calm wind kills the whole system (coupling lerps `0 → 1` at `RIDGE_WIND_REF_MS = 10 m/s`). HUD pill on the WIND row mirrors the chop pattern: amber `〰 RIDGE LIFT` once severity passes 0.2, upgrading to green `⬆ UPDRAFT` or orange `⬇ SINK` at the strong threshold (0.55) so the pilot reads direction at a glance, with the same 900 ms hold so a gap between adjacent pockets doesn't blink. One-shot PILOT ATC line per direction (`"Updraft, +500 fpm"` / `"Sink, -500 fpm"`, m/s → ft/min rounded to the nearest 100) the first frame each strong-pocket sign latches; both latches clear once the player drops below 0.15 severity so the next ridge crossing re-arms cleanly. `_ridgeResetLatches()` wired into `resetPlane()` alongside the chop reset so a fresh sortie starts with the strip silent. Pure additive — no new mesh, no new audio, no per-frame allocations — and engine-out glide planning suddenly has a real reason to hug the windward side of the ranges.

- [x] Wind tee + signal square at the airport — each field now has a low pivoting wind tee (~2 m horizontal arrow with a tail crossbar and a chunky cone head on a 1.6 m post) clustered with the existing windsock on the runway's left side, plus a flat 6×6 m painted signal square ~12 m further inboard. The tee yaws every frame to point its nose INTO the wind (the takeoff/landing direction), held steady on dead-calm to avoid jitter (mirrors the windsock convention). The signal square is a `MeshLambertMaterial`-mapped quad with a per-airport `CanvasTexture` showing a chunky arrow + the runway designator; it spins its `mesh.rotation.z` by 0° or 180° to match the wind-favoured end via the shared `_favoredRunwayFlip()` helper, and only repaints its canvas (`tex.needsUpdate = true`) when the designator string actually changes — almost always once per session. `updateWindTeesAndSquares()` rides alongside `updateWindSocks()` in the main per-frame update loop and is primed once at module load so the first frame already shows the right runway. Cluster reads as a real ICAO segmented-circle so a circling pilot can read wind direction AND active runway in one glance.

- [x] Rescue truck on crash — when the player crashes within ~2 km of any airport, a small red fire/rescue truck with a blue roof strobe now spawns at that field's apron and drives in a straight line toward the wreck at 25 m/s, arriving 30-60s later (duration clamped per the spec so a wreck right next to the apron still takes ~30 s and a 2 km wreck stops at 60 s). Reuses the apron loop-center math from `makeGroundVehicles()` so the spawn point sits on the same service shoulder as the tug + fuel truck, and the heading is set every frame off the live travel vector so the silhouette always reads as moving the right way. Strobe pulse is a 0.6 s sin² flash mirroring the existing amber service-vehicle beacons; a one-shot low/high sine "woo-woo" siren (520 Hz → 780 Hz) fires once when the truck closes inside 100 m of the wreck via `sfxReady()` so no new audio hardware is allocated. Truck mesh is built lazily on first crash, hidden in photo mode (timers keep ticking so it pops back into the right spot on photo exit), cleared by `stopReplay()` (player dismissed the post-crash replay) and `resetPlane()` (R / quick-restart / fresh mission). Carrier crashes deliberately skipped — no fire department at sea, and the rescue mesh doesn't float. Pure flavor — no collision, no rescue, no physics — but a wreck within sight of a field now feels like something the world is responding to instead of just "banner pops, restart".

- [x] Speed-unit setting (km/h ⇄ knots ⇄ mph) — new SPEED UNITS row in the settings panel (`<select id="setSpeedUnit">` next to AUDIO VOLUME) flips the displayed unit on every pilot-facing speed readout without touching the flight model. A new `SPEED_UNITS` table + `speedUnitEntry()` / `speedSuffix()` / `speedFromKmh()` / `speedFmt()` helpers do the conversion in one place; `settings.speedUnit` is persisted to localStorage with `kmh` as the default so existing flights look identical until the player opts in. Per-frame readouts (HUD `SPD`, best-glide IAS pill, post-touchdown status banner, crash report card speed, landing performance card speed, cockpit ASI gauge — dial scale, tick labels, stall + ops bands, needle, digital readout) all run through the helpers. Static markup (HUD SPD unit suffix, best-glide target `105`, checklist `SPEED <200 km/h`, ASI gauge `AIRSPEED km/h` label, two `~140 km/h` rotate-speed tips on the intro + tutorial cards) gets repainted by a new `applySpeedUnitText()` that's called once at boot and again every time the user picks a new unit. Wind readouts intentionally stay in knots — real-cockpit convention, and keeping them unchanged preserves the "default kmh, existing flights look identical" promise. Cockpit ASI dial geometry rebuilds in the chosen unit so major ticks stay round (0/50/100 kt, 0/50/100/.../250 mph), with the stall + green-band arcs anchored to their km/h equivalents so the warning stripes still mark stall (60 km/h ≈ 32 kt) and the cruise window (80-220 km/h ≈ 43-119 kt) at the same physical airspeeds.

- [x] Fuel endurance + time-to-empty readout — small `T/E mm:ss` pill in the FUEL HUD row that reads remaining burn time live off the same gph proxy `drawCockpitFuelFlow()` + the mini-map fuel range ring trust (`gph -> per-sec drain = gph/5760`), so the cockpit needle, the dashed ring, and this pill all agree on what "burn rate" means right now. Color tracks the existing fuel ramp: green at cruise, amber under 20 min, red under 10 min. Reads `T/E ∞` (cyan) when there's no burn — engine out, fuel cut, fuel exhausted, or pre-crank cold start — so the deadstick glide picture stays clean. Display is capped at `99:59` so a freshly-fueled idle (~96 min) can't blow the layout, but the underlying maths stay honest. Pairs with the fuel-range ring (distance) for a complete "can I make it / how long do I have" readout.

- [x] Ground shadows for AI plane + balloons — cloned the existing `planeShadow` ellipse-disc rig for the AI circuit plane and the four hot-air balloons via a new `entityShadows[]` registry + `updateEntityShadows()` tick right after `updatePlaneShadow()`. Each entity gets its own thin `THREE.Mesh` that **shares the existing `planeShadowTex`** (one GPU upload covers all five new shadows), positioned each frame at the entity's `(x,z)` over `terrainHeight()` with the same `polygonOffset` so it never z-fights runways/aprons/the carrier deck. AI plane shadow stretches with bank + pitch like the player rig and yaws to match heading; balloon shadows stay round (no bank, no yaw — a sphere envelope doesn't have a silhouette axis). All shadows fade + grow with AGL using the same `PLANE_SHADOW_MAX_AGL = 600` curve as the player and cull at that cap so high cruise pays nothing extra, then defer to each entity's own visibility flag so the existing 12 km AI draw distance + 7 km balloon view distance keep them off when the entity itself culls. Hidden in intro/replay/photo so the menu world, flyby cameras, and saved PNGs all stay clean. Lazy-built on the first tick after `balloons[]` is populated to avoid hoisting issues. Pure additive scenery — no flight model, no audio, no allocations per frame — and overhead sunrise photos now read like a real airfield instead of one floating shadow under the player.

- [x] Lightning + thunder over distant storm cells — pure scenery layer that fires only while `settings.rain` is ON. A new `state.lightning` scheduler picks a fresh strike every 3–6 min, lights a brief full-screen white-blue overlay (new `#lightning` div, `z-index:2` so HUD stays readable, `mix-blend-mode:screen`, hidden in photo via `PHOTO_HIDE_IDS`) that decays over ~120 ms — peak alpha scales 0.55→0.20 by cell distance (4 km bright crack, 8 km soft pulse) so the screen never goes pure white and instruments stay scannable through the flash. Thunder is queued through a tiny `pendingThunder[]` array with `playAt = now + distance/340*1000` so close strikes crack within a beat and far cells mumble in ~20–25 s late, then routed through a new `playThunderRumble(distance)` that builds a low-passed noise burst (cutoff drops from 240→100 Hz with distance, two-bump envelope: crack peak then long roll) plus a sub-bass sine sag (55→28 Hz) on the existing `sfxReady()` WebAudio context — zero new long-lived audio nodes. Scheduler + flash + queue all idle out frozen against photo / pause / intro / replay so saved PNGs and the menu stay clean, but the visible flash still drains its timer so it never hangs at peak between frames. Toggling rain off zeroes the schedule, on re-rolls a fresh 3–6 min window; `resetPlane()` wipes the queue + schedule so mission restart re-rolls the storm timeline. Pending-thunder queue capped at 6 so a long stormy session can't grow unbounded.

- [x] Visible runway markings — every ground airport runway now paints a pair of fat white aim-point bars (50 m × 5 m, ±9 m off the centerline) centered 300 m past each threshold (right at the existing PAPI/touchdown `aimX/aimZ` reference) plus a second pair of fixed-distance markers (22 m × 3 m, same offset) 150 m further inboard at 450 m past the threshold. Built in a new `paintAimAndFixedDist()` IIFE inside `makeAirport()`, slotted right after the TDZ bars so the painted markings stack cleanly: piano keys → TDZ bars → aim points → fixed-distance markers, all polygon-offset to sit above the runway plane + the centerline dashes without z-fighting. Per-pair guards skip the bars if their outboard edge would leave the pavement (so no bars hang in the dirt on narrow strips) and the fixed-distance markers self-disable when the runway is too short for both threshold pairs to fit without crowding midfield (`a.len < 1000` skips FDM). Carrier deck keeps its own markings (arrestor wires + touchdown box). Pure additive scenery — no new geometry per frame, single shared white material, ~30 LOC — and runways finally read like real paved fields from base leg instead of grey rectangles with edge dashes.

- [x] In-game screenshot gallery (`/` key) — every photo-mode capture (the existing `S` save + `Shift+S` 3-2-1 countdown paths) now also stashes a moderate-quality JPEG data URL (q=0.78, grabbed off the same HUD-free framebuffer the PNG download reads from) into a 12-entry ring buffer persisted to `localStorage` under `flightsim.gallery.v1`. A new `📸 GALLERY` modal opens with `/` (or via a new `GALLERY` button in the photo-mode overlay pill) and shows a responsive thumbnail grid (16:9 cells, `repeat(auto-fill, minmax(200px, 1fr))`) where each cell hover-reveals per-shot `DL` (re-download with a fresh timestamp) and `×` (delete) buttons, plus a `#index · 3m ago / 2h ago / yesterday` meta strip across the bottom. Click the bare thumbnail to re-download — same affordance the buttons give, but without needing the hover. The gallery hotkey is `/` (Shift+G was the roadmap pick but it already opens the cockpit Engine Monitor / EGT page, same precedent as the old `Y` ribbon swap when `T` was already used); the help strip + photo-mode hint were updated to match. `pushPhotoGallery()` is called after the toBlob download is kicked off so a localStorage quota error never blocks the actual save, and `savePhotoGallery()` retries on `QuotaExceededError` by trimming oldest entries one at a time until the write succeeds. The gallery div is added to `PHOTO_HIDE_IDS` so an open gallery never bakes into a freshly-taken screenshot. Gated against intro / replay / text inputs and Ctrl/Cmd/Alt chords (Cmd+/ stays the browser shortcut), Esc closes from the modal. Captured frames now have a home instead of disappearing into the Downloads folder after every save.

- [x] Throttle quadrant friction lock (`Shift+\`) — toggleable detent that mechanically freezes the throttle lever so cruise power can't drift from a stray W/S tap, a held key, or an accidental touch-slider drag. Toggled via `Shift+Backslash` (gated through `state.introOpen` like the carb-heat / parking-brake / cowl-flap latches), persisted to `localStorage` under `settings.frictionLock` so the choice survives reloads, and seeded back into `state.frictionLock` at boot so a habitually-locked pilot doesn't have to re-arm every session. While locked, the per-frame throttle input path skips the `KeyW`/`KeyS` branch AND ignores `touch.throttle` so neither the keyboard nor the mobile slider can move the lever; cold-start, engine-out, and crash branches all win (cold throttle pin / engine-out bleed / crash bleed) so the lock can't trap a stuck-throttle emergency. Visual feedback layers across HUD + cockpit: new amber `🔒` glyph rendered next to the THR readout on the HUD (photo-mode aware), and a thin brass collar `TorusGeometry(0.020, 0.0055)` mounted on the cockpit throttle lever pivot that pinches around the shaft only when the lock is set (gated by the same cockpit-cam-only block that drives `throttleGroup.visible` so it never leaks into chase/flyby/orbit). Toggle plays the existing `playParkingBrakeClick()` ratchet for a satisfying mechanical click and fires a one-shot `THROTTLE LOCKED · cruise hold engaged` status banner + PILOT `"Friction lock set."` ATC line on engage; release stays banner-only (`THROTTLE UNLOCKED`) so toggling off mid-flight doesn't spam the radio strip. Help strip got the `Shift+\` binding tacked onto the canopy entry; no other key conflicts (plain `\` still toggles the canopy via the split handler).

- [x] Engine start checklist overlay (#coldstartcard) — small bottom-right card that pops in during the cold-start ritual (after the intro closes, while the engine hasn't caught yet) listing the four real pre-crank steps with a green ✓ ticking in as each one is set. The roadmap idea originally listed Master ON / Mags BOTH / Throttle CRACKED, but this sim has no master switch, no mags toggle, and pins the throttle to 0 during cold-start — so the four steps were re-mapped onto what the existing cold-start system actually checks: 1. CANOPY CLOSED (`!state.canopyOpen` — the same condition `beginIgnition()` already hard-blocks with an ATC nudge), 2. PARKING BRAKE SET (`state.parkingBrake` — the one-shot "set parking brake first" nag that fires on first crank), 3. MIXTURE RICH (`state.mixture >= 0.85`, the existing `MIX_ATC_FULL_RICH` threshold), 4. PRIME CARB (`state.primed` + 30s shelf life, the Shift+I 1.5s hold). Hit all four and the footer flips from `0 / 4 · then press I` to a pulsing green `✓ CLEARED TO CRANK · press I`. Each row pips once via the existing `playCalloutChime()` on first OK transition, gated by a 4-bit `state.coldStartPipMask` bitmask so a parking-brake-on/off/on dance only chimes once per ritual; mask resets on `resetPlane()` and on every fresh `armColdStart()` so the next sortie re-arms the chimes clean. Card uses `position:fixed; right:12px; bottom:220px; width:208px` to slot above the mini-map (so it never fights the bottom-center crash/land report cards), `z-index:7` and `pointer-events:none` so it's pure HUD overlay (no physics side effects, can't intercept a stray click on the canvas or mobile buttons during the ritual). Slide-in/out animation is pure CSS — `transform:translateX(140%)` + `opacity:0` + `visibility:hidden` with a delayed-visibility transition trick that holds the element visible through the 320 ms slide-out before the browser flips it back to hidden. Visibility gated to `state.coldStart && !state.introOpen && !state.photoMode && !state.replayActive && !state.paused && !state.crashed && state.onGround` so the card stays out of screenshots, frozen replay frames, the pause menu, and the crash report; air-restart (deadstick X) deliberately doesn't get this card — a deadstick restart is about flying the plane, not walking a checklist. Added to `PHOTO_HIDE_IDS` as belt-and-braces for the photo-mode hide path. `updateColdStartCard()` is called right after `updatePreflight(false)` in the main loop, with a cheap visibility gate inside so post-takeoff frames pay basically nothing. The card is intentionally distinct from the existing Shift+K `updatePreflight()` panel — that one is a comprehensive pre-flight panel for the whole departure, this one is a focused four-row crank-cleared nudge that appears only when you're actually trying to start the engine.

- [x] Storm cell visualization on the weather radar — the radar now paints 1–3 drifting amber/red storm cells on top of the existing cloud blobs while `settings.rain` is on. New `state.stormCells` array seeds 1–2 cells immediately when rain switches on and tops up to a cap of 3 every ~15–40s, biased to spawn upwind so they drift INTO the scope rather than away. Cells drift on the actual `state.wind` vector at 0.7× (so they read as anchored weather, not pure cloud advection), live ~150–350 s, and ease in over 6 s on spawn + ease out over 4 s when rain toggles off so they melt off the scope instead of popping. Severity 0–1 picks amber (building, ~900–2100 m radius) vs red (mature, ~2100–3300 m), with a bright hot core dot on sev ≥ 0.7 so mature cells read as "this one's dangerous" at a glance. World radius maps to panel pixels (floor 8 px, cap 38 px) so a 3 km supercell takes a meaningful bite out of the scope without swallowing it. Cells reset on `resetPlane()` (mission restart re-rolls the storm timeline). Frozen-frame gates (photo/pause/intro/replay) freeze the drift + spawn clock so saved PNGs stay clean and a long pause doesn't expire every cell. Turns the radar from "vibe-only ornament" into something the pilot can actually steer around.

- [x] Animated control surfaces (ailerons + rudder + elevator) — NEXT entry was a duplicate of the already-shipped "Animated aileron / elevator / rudder surfaces" item above. The `aileronL/aileronR/elevator/rudder` pivots already lerp toward live effective stick input (`rIn/pIn/yIn`, which include AP overrides + trim) at `SURFACE_RATE = 7/sec`, mirror the ailerons across the wing centerline, clamp to ~22° aileron/elevator and ~28° rudder, snap back to neutral on `resetPlane()` / quick-jump, and are visible in every external cam (chase, flyby, orbit, replay, photo) while being hidden in cockpit cam by the existing airframe-hide loop. Closed out without re-implementation so the same feature doesn't ship twice.

- [x] Tower flyby horn / wave — buzz an airport tower at low altitude and the sim now notices: when the plane crosses inside 500 m horizontally of the airport's beacon tower with AGL between 5 m and 200 m (the 5 m floor keeps taxi + landing rollouts out of the trigger), a one-shot `TWR "Nice pass, <id>!"` ATC line fires alongside a cheerful two-note triangle horn chirp (E5 → A5 ascending, deliberately the opposite of the descending square gear horn so the ear instantly reads "buzz" not "warning") and a small `🛬 +1 FLYBY · Buzzed <name> tower.` corner toast. Per-airport 30 s cooldown via `state._flybyCooldown` (lazy-init keyed by airport id so future fields cost nothing) so a tight circuit pattern can't spam it but a multi-stop tour can still pop one at each field. Tower world position is computed inline from `(a.w/2 + 9, a.len/2 - 18)` rotated by `a.hdg` — same math the `TOWER` camera and `makeAirportBeacon()` use, so the trigger geometry sits exactly under the beacon tower mesh the player can see from the air. Lifetime counter persists to `pilotStats.flybys` and shows up as `🛬 TOWER FLYBYS` in the Pilot Stats panel. Gated by the standard crash/replay/photo/intro/pause mask so frozen frames + intro animations stay clean. Carrier deck is excluded — no tower out there, and the carrier id isn't in `AIRPORTS` so the loop naturally skips it.

- [x] Sunset golden-hour tinted screenshots — during the dawn (~05:30–06:30) and dusk (~19:00–20:00) windows of `state.todHours`, photo-mode saves now bake a subtle warm color grade into both the downloaded PNG and the gallery JPEG: orange lift in highlights (+18·strength on R, +6·strength on G when luminance > 0.6), pulled-back blues in shadows (−22·strength on B when luminance < 0.4), and +5% saturation at peak (pivoted around per-pixel Rec.601 luminance). New `goldenHourStrength(h)` helper plateaus at 1.0 inside the two windows and ramps linearly across a 30-minute shoulder on each side, so the grade fades in/out cleanly with the cycle instead of snapping on. `savePhoto()` routes the framebuffer through an offscreen 2D canvas (`drawImage(canvas)` → `getImageData` → per-pixel pass → `putImageData`) only when strength > 0; outside the windows it takes the legacy fast path with zero per-pixel work. Any failure (tainted canvas, context loss, OOM on huge frames) falls back to the raw WebGL canvas so the player still gets their PNG, just without the grade. New `#goldenPill` (`🌇 GOLDEN HOUR`, warm amber border + glow) sits top-center under any HUD strip while in photo mode + a window is active so pilots know the bonus is live; `tickGoldenHourPill()` runs from the photo-mode branch of the main loop + a one-shot sync from `setPhotoMode(true)` so it reads right on frame 1, and is force-hidden by `savePhoto()`'s overlay sweep so the pill itself never bakes into the shot.

- [x] Skid ball / inclinometer in the cockpit panel — new `cpSkid` round gauge sits between the magnetic compass and the brake-temp gauge in the cockpit panel. Inside the bezel a shallow curved tube spans the lower face with two white reference posts at center and an amber glass ball (radial gradient highlight, dark rim) that drifts along the arc opposite the body-frame lateral acceleration: a left skid pushes the ball right ("step on the right rudder"), a right slip pushes it left, and centered coordinated flight parks it between the posts. New `state.slipBall` is published from the physics step right after `beta` is computed — in flight it tracks pure sideslip (`-(beta / 0.35)`), at low ground speed it falls back to a `vBody.x`-derived lateral-G proxy so the ball still drifts during taxi turns, EMA-smoothed (`min(1, dt*5)`) and clamped to ±1.6 so a hard slip pins the ball at the tube wall instead of running off the face. Self-test override sweeps the ball from the right wall back to center during avionics power-up to match the other gauges' dance. Coaching cue (`L RUDDER` / `R RUDDER`) whispers in below the tube only when off-center > 22% and the plane is airborne so taxi turns don't nag, amber inside the warning band and orange-red past 60% deflection. ~110 LOC of pure visual readout — nothing in the physics depends on it, gauge inherits the existing night / dusk / alt-fail / alt-dark filter chain via the standard `_cpBezel` bezel + canvas class wiring, no new audio, no new state mutation, no new constants.

## NEXT — pick the top item each loop
Ranked by impact-per-LOC. Top of the list wins next ship.

- [ ] Voice ATIS broadcast on tune-in (`Shift+A`) — press `Shift+A` near any airport (within 8 km) to pop a small bottom-left card that auto-fills the current wind direction + speed, altimeter (synth pressure tied to TOD), visibility (clear / overcast based on rain), active runway (heading-aware via existing `runwayDesignator()`), and a generated ATIS identifier letter that rotates A→B→C... over the session. Two-second voice-style synth chirp on tune so the pilot knows it's live, fades after 8 s or another `Shift+A` press dismisses early. Turns the existing wind sock + windshear + rain systems into a single pre-approach briefing players actually consult, no radio nav complexity.

- [ ] Engine torque roll on takeoff — at high throttle (>80%) below 80 km/h on the ground, the airframe now picks up a subtle left-rolling tendency (~0.04 rad/s aileron bias to the left, scaled by throttle) that the pilot has to counter with right rudder + a touch of right aileron. Real prop planes do this because of P-factor + spiraling slipstream + torque reaction, and it's the single most-missed takeoff cue for student pilots. Pairs with the existing crosswind aileron coaching and rudder trim so the takeoff roll finally feels like flying a real airplane instead of a rail. Bias smoothly fades to zero past 100 km/h so cruise + climb are unaffected.

- [ ] Live wind barbs on the mini-map — at each airport on the mini-map, overlay a tiny meteorological wind barb (the standard pennant + full-bars + half-bars notation: each full bar = 10 kt, half = 5 kt, pennant = 50 kt) pointing FROM the wind direction. Each airport reads its local wind (currently all 3 fields share the same global wind, but the barb system is the right primitive for when per-field wind lands). Cleaner than the existing single corner arrow because pilots can see which field has favorable winds at a glance for diversion planning. Reuses the existing wind state and mini-map canvas; ~25 LOC.

- [ ] Engine sputter on fuel exhaustion — at <2% fuel remaining, the engine starts coughing instead of running smooth: random ~0.5 s thrust dropouts every 3–6 s, layered with a wet-sputter SFX (low-pass noise burst + brief square chirp at the prop frequency), accompanied by a one-shot PILOT ATC "Engine surging, fuel low" + an amber `⚠ FUEL STARVED` HUD pill on the FUEL row. Past 0% fuel, sputters get more frequent and finally cut to full engine-out. Gives players a meaningful 30–60 s warning window before total failure instead of the current cliff-edge cut, and pairs with the existing fuel gauge flash + ATC calls so the whole low-fuel sequence finally tells a story.

## How the ship loop works
Every 5 min during awake hours, an isolated agent runs:
1. Reads this ROADMAP.
2. Picks the top unchecked item under NEXT.
3. Edits `index.html` to land that feature (and only that).
4. Tests by opening with a headless check (HTML parses, no console errors in static analysis).
5. Commits + pushes to `main`. GitHub Pages auto-deploys.
6. Marks the item `[x]` and moves it to DONE in this file (same commit or follow-up).
7. Reports a one-line summary back, then exits.

If everything in NEXT is done, the agent appends 5 fresh ideas to the bottom of NEXT (impact-ranked) before picking one.
